# TRUTH_LAYER

Project: `clandestine-fulfillment`
Root: `/Users/Shared/WorkShared/Project/clandestine-fulfillment`

## Purpose

This is the canonical source of architecture truth for planning, building, and auditing.
If this file conflicts with ad-hoc notes, this file and linked truth docs win.

## Required Read-First Set (Hard Block)

Before producing PLAN, BUILD, or AUDIT output, read:

1. `TRUTH_LAYER.md`
2. `docs/system_map/INDEX.md`
3. `docs/system_map/API_CATALOG.md`
4. `docs/system_map/TRIGGER_TASK_CATALOG.md`
5. `docs/system_map/CACHE_ARCHITECTURE.md`
6. `project_state/engineering_map.yaml`
7. `project_state/journeys.yaml`
8. `docs/RELEASE_GATE_CRITERIA.md`
9. `docs/RUNBOOK.md`

If any required file is missing or stale for the requested scope, return `BLOCKED` and list what must be updated.

## Core System Invariants

- UI data access uses `useAppQuery` / `useAppMutation` with `query-tiers`.
- Every new read path must follow `docs/system_map/CACHE_ARCHITECTURE.md` (tier class, scope-safe keying, invalidation-first freshness).
- Server-side auth context comes from `requireAuth()` and validated role checks.
- Workspace-scoped admin reads must derive tenant scope on the server from `requireAuth()` rather than trusting a client-cached workspace id for the primary row filter.
- Admin query surfaces must not collapse auth/query failures into the same empty-state copy used for true zero-row results.
- Admin settings pages should avoid hard dependencies on separate bootstrap context reads when the primary domain actions can derive workspace scope from auth directly.
- Cold-config admin pages whose initial reads require request-bound auth context may server-render their initial dataset and then use client-side mutations plus `router.refresh()` for freshness instead of invoking read actions from client query hooks during bootstrap. `/admin/settings/store-connections` now follows this pattern to avoid post-OAuth request-scope failures during page render.
- **SKU matching workspace (2026-04-25):** `/admin/settings/sku-matching` is the staff-only alias-review surface for reconciling canonical warehouse variants to Shopify / WooCommerce / Squarespace product identities without rewriting remote SKUs. Read model is connection-scoped and server-bootstrapped (`getSkuMatchingWorkspace`) behind the workspace flag `sku_matching_enabled`; interactive actions use `router.refresh()` instead of client boot reads. Candidate ranking is deterministic-ID first (existing mapping, exact SKU, exact barcode), then secondary signals (artist/vendor text, canonical/Bandcamp title, format, variant/option text, price), with explicit disqualifiers for rows that only have weak title evidence or no remote SKU/barcode at all.
- **SKU matching persistence contract (2026-04-25):** `client_store_sku_mappings` remains the live alias table consumed by downstream fanout/webhook code, but now carries lean current-state provenance (`match_method`, `match_confidence`, `matched_*`, `deactivated_*`, `candidate_fingerprint`). Heavy audit history lives in `sku_mapping_events` (append-only). All single-row match writes MUST go through the `persist_sku_match` RPC so active-row uniqueness, soft-deactivation of superseded rows, and audit-event insertion stay atomic.
- **Phase 0 duplicate audit requirement (2026-04-25):** before adding or changing active-row unique indexes on `client_store_sku_mappings`, operators must run the duplicate audit (`scripts/audit-sku-mapping-duplicates.sql` or `scripts/audit-sku-mapping-duplicates.ts`) and the remediation script dry-run/live path. Current production audit artifact: `reports/sku-matching-duplicate-audit-20260425T1337Z.json` with zero connection-variant, remote-inventory-item, remote-variant, and canonical-SKU duplicate groups.
- **Shopify readiness in SKU matching (2026-04-25):** a Shopify alias is not operationally ready unless the connection has `default_location_id`, the mapping has `remote_inventory_item_id`, and the item is stocked at that default location. The read model classifies this as `ready_at_default_location | missing_default_location | missing_remote_inventory_item_id | not_stocked_at_default_location | location_read_failed`. Staff can activate a matched inventory item at the default location via the shared Shopify `inventory_levels/connect.json` path (`activateShopifyInventoryAtLocation`) from the SKU matching review drawer; activation logs `source='inventory_activate'` audit rows.
- **SKU matching monitoring contract (2026-04-25):** the workspace is read-mostly, but rollout safety still depends on telemetry. `getSkuMatchingWorkspace()`, `previewSkuMatch()`, `createOrUpdateSkuMatch()`, `deactivateSkuMatch()`, and bulk accept emit best-effort rows to `sku_matching_perf_events`. Weekly Trigger task `sku-matching-monitor` aggregates the trailing 7 days into `sensor_readings` (`sensor_name='sku_matching.monitoring'`) and opens a review-queue item when conflict growth crosses the rollout threshold, so operators can pause expansion before alias drift accumulates.
- **Autonomous SKU matching substrate (Phase 0 + Phase 1 + Phase 2 substrate, 2026-04-26):** the autonomous-matching rollout is layered ON TOP OF the existing `client_store_sku_mappings` alias table, NOT a replacement. The plan lives at `/Users/tomabbs/.cursor/plans/autonomous_sku_matching_da557209.plan.md`; advancement is gated by `docs/RELEASE_GATE_CRITERIA.md §C.5 (SKU-AUTO-*)` — 32 additive gates on top of `SKU-1..5`. Invariants that are ACTIVE today (enforced by migrations `supabase/migrations/20260428000001_sku_autonomous_matching_phase0.sql` + `20260428000002_sku_autonomous_matching_phase1_rpc.sql`, CI, or unit tests):
  - **Fanout isolation (SKU-AUTO-1):** `client_store_product_identity_matches` is an identity-only table. Its rows are NEVER consumed by `src/lib/server/inventory-fanout.ts`, `src/lib/server/client-store-fanout-gate.ts`, `src/trigger/tasks/multi-store-inventory-push.ts`, or `src/trigger/tasks/process-client-store-webhook.ts`. Fanout reads ONLY `client_store_sku_mappings`. The grep guard `scripts/ci-checks/sku-identity-no-fanout.sh` (wired into `scripts/release-gate.sh` + `scripts/cloud-agent-verify.sh`) fails the build if any of those four files ever references the identity table.
  - **Cross-workspace scope trigger (SKU-AUTO-26):** `enforce_identity_match_scope()` is attached to BOTH `client_store_product_identity_matches` AND `client_store_sku_mappings`. Any INSERT/UPDATE where `row.workspace_id ≠ connection.workspace_id`, `row.org_id ≠ connection.org_id`, or `variant.workspace_id ≠ row.workspace_id` is rejected at the trigger level — autonomous matching cannot leak rows across tenants even via service_role.
  - **Outcome-state single source of truth (SKU-AUTO-6):** `STORED_IDENTITY_OUTCOME_STATES` in `src/lib/server/sku-outcome-transitions.ts` is the canonical TypeScript mirror of the `client_store_product_identity_matches.outcome_state` CHECK. `FULL_OUTCOME_STATES = STORED ∪ { 'auto_live_inventory_alias' }` because `auto_live_inventory_alias` lives on `client_store_sku_mappings`, NOT on identity rows — the state machine straddles two tables. `tests/unit/lib/server/sku-outcome-transitions.test.ts` reads the migration SQL at runtime and diffs the CHECK list against the TS sets — drift = red build.
  - **Identity row variant nullability (SKU-AUTO-12):** `auto_database_identity_match` and `auto_shadow_identity_match` rows MUST carry a `variant_id`; `auto_reject_non_match` rows MUST NOT. Enforced by `client_store_product_identity_matches_variant_nullability_check` at the DB level.
  - **Remote-listing uniqueness (SKU-AUTO-13):** three partial unique indexes on `client_store_product_identity_matches` — `(connection_id, remote_product_id, remote_variant_id)` + `(connection_id, remote_inventory_item_id)` + `(connection_id, remote_fingerprint)` — guarantee exactly one identity row per remote listing per connection. The fingerprint is the escape hatch for platforms with unstable remote IDs.
  - **Remote fingerprint contract (SKU-AUTO-25):** `src/lib/server/remote-fingerprint.ts::buildRemoteFingerprint()` returns a SHA-256 hex digest of the deterministically key-sorted JSON of the `MusicVariantDescriptors` AST produced by `parseMusicVariantDescriptors()`. Frozen fixtures in `tests/unit/lib/server/remote-fingerprint.test.ts` lock the hash values so a descriptor-parser regression cannot silently re-shuffle every identity row.
  - **Stock-tier reliability (SKU-AUTO-30):** `classifyStockTier()` in `src/lib/server/stock-reliability.ts` downgrades any non-authoritative signal with `|clock_skew_ms| > 1h` to `cached_only` regardless of claimed freshness. `atpOf(signal, committed, safetyStock)` is the only sanctioned path from raw stock signal to "available to promise" — stock-based tiebreaks and promotion decisions must route through it.
  - **Placeholder-SKU hard disqualifier (Phase 1 — plan §Ranking contract):** `isPlaceholderSku()` in `src/lib/shared/utils.ts` returns true for the empty-set `{ '', '-', '--', '0', 'n/a', 'na', 'none', 'null', 'tbd', 'tba', 'unknown', 'placeholder', 'default', 'test', 'sample' }`, 1-3 digit numeric strings, and the Squarespace `SQ#####` auto-generated pattern. Placeholder SKUs can never be promoted to `auto_live_inventory_alias` and can never be the deciding evidence for `auto_database_identity_match` — they exist in the descriptor AST only to survive the fingerprint hash.
  - **Kill switch substrate (SKU-AUTO-28 column active, plumbing Pending-Phase-2):** `workspaces.sku_autonomous_emergency_paused` + `sku_autonomous_emergency_paused_at` + `sku_autonomous_emergency_paused_by` exist today. Phase 2 adds the `checkEmergencyPause()` guard that every autonomous runner calls before starting a new run.
  - **State-transition RPC (SKU-AUTO-14 + SKU-AUTO-22):** `apply_sku_outcome_transition(p_identity_match_id, p_expected_state_version, p_expected_from_state, p_to_state, p_trigger, p_reason_code, p_evidence_snapshot, p_triggered_by)` is the ONLY sanctioned path that mutates `client_store_product_identity_matches.outcome_state`. The function takes `pg_advisory_xact_lock(hashtext('sku_transition:' || id))` as its first effectful statement (absorbs webhook thundering herds per-row), then OCC-checks `state_version`, drift-checks `expected_from_state`, enforces terminal-state egress via `human_review` only, REFUSES to write `auto_live_inventory_alias` to the identity row (that state lives on `client_store_sku_mappings` and is written by `promote_identity_match_to_alias`), UPDATEs the row (`state_version += 1`, `evaluation_count += 1`), INSERTs one `sku_outcome_transitions` audit row in the same txn, and RETURNS `(new_state_version, transition_id)`. The TS wrapper `applyOutcomeTransition()` in `src/lib/server/sku-outcome-transitions.ts` calls `validateOutcomeTransition()` client-side for defense-in-depth before invoking the RPC and maps every documented exception back to a typed reason (`stale_state_version`, `from_state_drift`, `terminal_state_non_human_egress`, `to_state_forbidden_on_identity_row`, `identity_match_inactive`, `identity_match_not_found`). Migration-shape drift guard + 15 wrapper unit cases + live-DB concurrency test (`tests/integration/sku-outcome-transition-concurrency.test.ts`, N=8 parallel callers) provide the SKU-AUTO-14 / SKU-AUTO-22 evidence.
  - **Bandcamp linkage phase gate (SKU-AUTO-29 RPC active):** `compute_bandcamp_linkage_metrics(workspace_id, org_id?)` returns `{ linkage_rate, verified_rate, option_rate, pool_count, linked_count, verified_count, optioned_count }`. Phase 2 advancement requires `30% / 20% / 10%`; Phase 5 requires `50% / 40% / 25%`; Phase 7 requires `70% / 60% / 40%`. Operators confirm thresholds are reachable per target workspace via `scripts/_sku-matcher-linkage-baseline.ts` before flipping `sku_identity_autonomy_enabled`.
  - **Order-hold as a distinct state (Phase 3 — gate SKU-AUTO-10):** `warehouse_orders.fulfillment_hold text DEFAULT 'none' CHECK ∈ { 'none', 'on_hold', 'released' }` is SEPARATE from ShipStation's `on_hold` or from `cancelled`. No downstream consumer is permitted to coerce it. Every transition writes a row to `order_fulfillment_hold_events` in the same transaction; the `hold_cycle_id` + `event_kind CHECK ∈ { 'hold_applied', 'hold_released', 'hold_alert_sent', 'hold_alert_skipped' }` pair guarantees one `hold_applied` + one `hold_released` per cycle, with alert dispatch idempotent per cycle.
  - **Autonomous run auditability (SKU-AUTO-18):** every autonomous decision writes a `sku_autonomous_runs` header row plus one `sku_autonomous_decisions` row per candidate pair. Dry-run mode (Phase 1) populates BOTH tables but writes nothing to identity or alias tables and dispatches no hold events — the audit surface is identical to production, and the rollout cannot be "flipped on" via a config change without producing inspectable dry-run evidence first.
  - **Normalized order adapter + hold substrate (Phase 2.A/2.B, 2026-04-26):** `src/lib/server/normalized-order.ts::buildNormalizedOrder()` + `normalizeWebhookOrderFromLines()` are the ONLY sanctioned producers of `NormalizedClientStoreOrder` — the union type consumed by both the webhook path (`process-client-store-webhook`, later `process-shopify-webhook`) and the scheduled-poll path (`client-store-sync-orders`). `loadNormalizedOrder(supabase, order_id)` hydrates by ID from `warehouse_orders` + `warehouse_order_items` with deterministic line ordering and connection-consistency assertion. `src/lib/server/order-hold-policy.ts` is the PURE policy: `classifyOrderLine(line, state)` + `decideOrderHold(classifications)` + `buildHoldDecision(...)` map `(remoteSku, aliasRow, identityRow, warehouseAvailable, fetchStatus)` to a `HoldDecision` (`shouldHold`, deterministic `holdReason` via `HOLD_REASON_PRIORITY`, `affectedLines`, `committableLines`, `clientAlertRequired`, `staffReviewRequired`) — SKU-AUTO-3 (webhook/poll path consistency) is satisfied by both paths lowering to `NormalizedClientStoreOrder` before entering this policy. `src/lib/server/order-hold-evaluator.ts::evaluateOrderForHold(supabase, order)` is the async orchestrator that batch-fetches alias/identity/fetch-status/inventory in a bounded number of queries (one per table for all lines, never N×lines) and hands the result to the pure policy. Companion tests: `tests/unit/lib/server/normalized-order.test.ts`, `normalized-order-loader.test.ts`, `order-hold-policy.test.ts`, `order-hold-evaluator.test.ts`.
  - **Structured candidate evidence + gate sequence (Phase 2.C, 2026-04-26):** `src/lib/server/sku-candidate-evidence.ts` is the PURE evidence builder + gate classifier that replaces "one weighted score" with explicit identity → variant → operational gates (plan §1691–1740). `buildCandidateEvidence(input)` constructs a `CandidateEvidence` struct from canonical/remote descriptors + identity/operational/negative signals. `classifyEvidenceGates(evidence, {platform})` evaluates the three gates in plan-order and returns `EvidenceGateResult { identity, variant, operational, overall, disqualifiers[] }`; `overall` is one of `{ pass, identity_only, stock_exception, shadow_identity, holdout, reject }` and is the authoritative input for `selectOutcomeFromGates()` → `IdentityOutcomeState`. Hard negatives (`placeholderSku`, `duplicateSku`, `duplicateRemote`, `nonOperationalRow`) force `overall='reject'` unconditionally — they cannot be overcome by any amount of positive evidence (plan §1759–1769). Variant disagreements (e.g. `variant_format_disagrees`, `variant_color_disagrees`) also force `overall='reject'`; `"unknown"` on a variant slot is NOT disagreement and routes to `shadow_identity`. `src/lib/server/sku-matching.ts::rankSkuCandidates(canonical, remotes, evidenceContext?)` is ADDITIVE — existing callers that omit `evidenceContext` see the exact legacy shape; new callers that supply it receive `evidence`, `evidenceGates`, and `disqualifierCodes` on every `RankedSkuCandidate`. Companion tests: `tests/unit/lib/server/sku-candidate-evidence.test.ts` (50 cases) + `sku-matching.test.ts` (additive evidence suite, 5 new cases).
- Org/workspace sensitive writes are protected by RLS or service-role actions with explicit authorization checks.
- Trigger.dev handles background and asynchronous workflows; debugging must include related tasks.
- **Support Inbox 2.0 (2026-04-25):** support queues are computed views over canonical conversation fields (`status`, `assigned_to`, `category`, `priority`, `snoozed_until`, SLA timestamps), not persisted queue names. `support_messages.client_mutation_id` provides retry-safe send idempotency; stale replies are hard-blocked unless the staff UI explicitly sends with `forceSendAfterCollision`. External sends are never fire-and-forget: staff replies create `support_message_deliveries` rows first, then `support-message-delivery` sends email/Discogs and `support-delivery-recovery` retries failed rows. SLA pause/resume accounting is owned by the Postgres trigger `support_sla_pause_on_status_change()` so all update paths pause while `waiting_on_client` consistently.
- **Support Inbox database evidence (2026-04-25):** `/admin/support` summary must surface DB-backed total ticket/message counts plus latest-message/loaded timestamps, and conversation cards must show per-thread message counts. Queue counts use queue membership (`status='resolved'` for the Resolved queue), not incidental fields such as `resolved_at`, so imported/rescued support rows cannot appear in a list while the queue badge says zero.
- Webhook handlers must preserve idempotency and bounded retries.
- First-party Shopify webhook ingress (`/api/webhooks/shopify`) is observe-only for inventory/order topics (`inventory_levels/update`, `orders/create`, `orders/updated`). ShipStation is authoritative for order/inventory movement; Clandestine Shopify product sync remains Graph API task-driven.
- **Webhook ingress empirical envelope (Phase 4 Sub-pass A.x complete, 2026-04-24, against Northern Spy `cutover_state=legacy`):** Production Node ingress route handler is healthy on the OK path (median ~225–280 ms, p95 1,507 ms) but **fails F-7 (cold-start p95 < 800 ms) by ~2x**. Theoretical bottleneck under sustained burst is the synchronous `tasks.trigger()` call to Trigger.dev v4's enqueue API: empirical ceiling = **27.5 enqueues/sec sustained at concurrency 10, hard cliff at concurrency 15** (89% saturation), with **penalty-box behavior at concurrency ≥20** (`ms_to_first_503=0` across 4 consecutive steps; rate-limit window suspected ≥5-min). HRD-17.1 graceful degradation (`enqueue_failed` 503 → `webhook-events-recovery-sweep` retries within 5 min + Shopify retries on 503 with backoff) means data loss is impossible. **OPERATIONAL REALITY (X-1.b verification, 30 days production data, 66,415 webhook events): ZERO `enqueue_failed` rows ever occurred in production. Worst single-second peak = 30 rps (one event 2026-04-20T15:37); worst sustained 60s rate = 13.1 rps; worst 5-min rate = 10.3 rps — all below the 27.5 ceiling.** Production bursts are too short to consume the rate-limit bucket. **Decision (X-1-decision-DRAFT.md)**: Phase 4 §9.5 build (Edge migration + waitUntil + batchTrigger) is **DEFERRED** — no operational signal currently justifies the engineering investment. Re-open if any of: enqueue_failed > 0 for 3+ consecutive days, single-second peaks ≥30 rps in 5+ separate hours within a week, sustained 60s rate ≥20 rps, Vercel cold-start p95 >2,500 ms, Shopify webhook subscription deactivation, or new platform integration adding 3-5× volume. Recommended: weekly automated re-run of `_phase4-x1b-status-verification.ts` as defensive monitor.
- **Webhook pipeline health remediation (2026-04-20):** the 2026-04-20 workspace-first stabilization (engineering_map prior_update, `src/app/api/webhooks/shopify/route.ts` deterministic workspace resolution + observe-only mode for inventory/order topics) **decisively eliminated** a fire that produced 33,528 `parse_failed` + 22,902 `pending` rows between 2026-04-09 and 2026-04-20 (~84% of all webhook_events in that window). Post-stabilization (2026-04-21 onward): zero new parse_failed, zero new pending. 5 residual `pending` stragglers from 2026-04-21/22 are deferred for cleanup investigation (`webhook-events-post-stabilization-stragglers`). Apr 23-24 traffic (8,400+ events) shows clean `received` status flow — the stabilization is operating as designed.
- Release confidence is enforced by `release-gate` checks and full-site audit criteria.
- Bundle inventory uses component-level tracking via `bundle_components` table. When a bundle sells (any channel), `bundle-component-fanout` decrements each component SKU. Inventory pushes compute MIN(bundle_stock, floor(component_stock/qty)) for effective availability. `bundles_enabled` flag is advisory; `bundle_components` row existence is the source of truth.
- Bandcamp follows an **authority lifecycle**: Bandcamp API is authoritative for **initial ingest** (new titles, SKU/quantity/date/price bootstrap). After staff review or physical count, the warehouse app becomes authoritative for **operational fields** (SKU, quantity, price, dates). Bandcamp remains authoritative for **descriptive/external fields** (URL, subdomain, album_title, options, sales data) permanently. Governed by `authority_status` on `bandcamp_product_mappings` (`bandcamp_initial` → `warehouse_reviewed` → `warehouse_locked`). HTML scraping (`data-tralbum`) is **enrichment only** (about, credits, tracks, package photos). Automation is **bounded** (caps, DLQ to `warehouse_review_queue`, no unbounded retry).
- **Org-id backfill on warehouse_products (2026-04-20):** historical shopify-sync runs left `warehouse_products.org_id = NULL` on 1,533 of 4,478 products (35%), even though the schema declares the column NOT NULL — those rows pre-date the constraint or were inserted via service-role paths that bypassed it. `scripts/backfill-product-org-id.ts --apply --create-orgs` resolves every NULL via a tiered priority — (1) bandcamp connection link `bandcamp_product_mappings.bandcamp_member_band_id → bandcamp_connections.org_id` (23 products), (2) exact case-insensitive match `warehouse_products.vendor == organizations.shopify_vendor_name` (0, because no org had it set), (3) exact match against `organizations.name` (1,496 products), (4) normalized fuzzy match — lowercase + drop `records|recordings|label|tapes|the|and` + strip punctuation (1 product), (5) multi-vendor split on `|`, `/`, `&`, `+` taking first matching half (0), (6) auto-create with `--create-orgs` for genuinely new labels (13 products → 2 new orgs: `Framework`, `Croux records`). Final state: 1,533 → 0 NULL `org_id`. **Side effect**: the script also writes `organizations.shopify_vendor_name = vendor` (idempotent — only when currently NULL) for every org resolved via name/normalized match (162 orgs touched), so future shopify-sync runs hit the canonical vendor key and never silently leave NULL again. Tier ordering matters: bandcamp wins over vendor because the bandcamp account → org relationship is a direct DB link curated by staff during onboarding, while `vendor` is free-text from Shopify. Read-only by default; CSV plan + unresolved + JSON summary land in `reports/finish-line/org-backfill/`.

- **Inventory master export workbook (2026-04-20):** `scripts/export-inventory-master.ts` produces a single XLSX (`reports/inventory-master/inventory-master-<ts>.xlsx`) joining `warehouse_product_variants` ⨝ `warehouse_products` ⨝ `organizations` (LEFT joins so NULL-org rows are visible, NOT inner — inner silently drops half the catalog) plus `warehouse_inventory_levels`, `warehouse_variant_locations`, `bandcamp_product_mappings`, and `bandcamp_connections.band_name`. Four sheets: `Inventory Master` (one row per variant — SKU, Format, parsed Artist, parsed Title, Variant Detail, Account/Label, Vendor, System Available, blank `NEW COUNT` column for staff, Locations, Price, Cost, Compare-At, Barcode, UPC, Weight, Bandcamp Type/URL/Push Mode, clickable Shopify Admin URL, Status, Tags, plus auto-detected `Missing Fields` and a blank `Notes` column; trailing `_variant_id`/`_product_id`/`_org_id`/`_workspace_id` columns are stable keys for re-import matching), `Validation Summary` (counts of every gap by category + top 30 labels + format/status distributions), `Locations Reference` (every `warehouse_locations` row), `README` (column guide + reimport contract). Artist/Title parser priority: bandcamp `bandcamp_album_title` substring → `" - " / " — " / " – "` split → vendor as fallback (each tagged with confidence). PostgREST `.in()` calls chunk at 200 IDs to stay under URL length limits. Read-only — no DB writes, no external API calls. Companion importer (`scripts/import-inventory-master.ts`) is deferred until staff complete the count; it will match by `_variant_id` (primary) then `workspace_id+SKU` (fallback) and route every `NEW COUNT` delta through `recordInventoryChange({ source:'manual_inventory_count' })`, preserving Rule #20.

- **Inventory-master backfill — distro-only SKU import + format completion (2026-04-20):** the five Pirate Ship / shop inventory master spreadsheets (`avant 1.xlsx`, `redeye over.xlsx`, `gr.xlsx`, `anacortes.xlsx`, `Master page.xlsx`) carry distro SKUs that never reached Bandcamp (back catalog, color variants of vinyl per Rule #8, multi-size apparel, posters/patches/totes). The 115 `no_match` SKUs identified by `scripts/audit-stale-inventory-vs-db.ts` were imported via `scripts/import-missing-skus.ts --apply --create-orgs` (read-only by default; idempotent — pre-flight `SELECT` collision check + DB rollback if Shopify create fails). The script (a) groups SHIRT-prefixed SKUs that share a prefix-without-size-suffix into one multi-variant Shopify product (Rule #8 apparel), (b) creates one product per SKU otherwise, (c) resolves `org_id` via existing-vendor → exact org name → fuzzy match (composite labels split on `/`), (d) auto-creates orgs for unrecognized labels (Telegraph Harp, Fish of Milk, Odd Cat) when `--create-orgs` is set, (e) tags every Shopify product `import:inventory-master:2026-04-20`, (f) sets `warehouse_products.status='draft'`, (g) uses `productSetCreate({ status:'DRAFT' })` via the existing full-shape builder, (h) backfills `shopify_product_id` / `shopify_variant_id` / `shopify_inventory_item_id` via `fetchProductVariantsByProductId()`, (i) **imports zero inventory** — quantities are out of scope. Final delta: 73 new `warehouse_products`, 100 new `warehouse_product_variants`, 73 Shopify drafts, 3 new orgs; 10 sheet rows remain `no_match` (4 junk + 3 missing-title + 3 ambiguous duplicates) and are documented in `reports/finish-line/import-plans/import-skipped-*.csv`. Companion script `scripts/backfill-format-name.ts --apply` resolved 281 of 281 NULL `format_name` values (after this pass: 0 NULL across 3,046 variants) using the priority order: format-bearing SKU prefix (`LP-`/`CD-`/`CS-`/`2XLP-`/`MLP-`/`7IN-`/`SI-`/`TS-`/`SHIRT-`/`TB-`) → parent product title keyword → `bandcamp_product_mappings.bandcamp_type_name` → category SKU prefix (`BOOK-`/`BAG-`/`POSTER-`/`PATCH-`/`MERCH-`/`FRAME-`/`EB-`/`MAG-` → `Other`) → inventory-master XLSX SKU cross-reference → Shopify `product_type` fallback. The shared regex helper `bandcampTypeNameToFormat()` (in `src/trigger/lib/format-detection.ts`) checks `7"`/`Cassette`/`CD` BEFORE `LP`/vinyl to avoid `7" Vinyl` and `Cassette Tape` Bandcamp type strings landing on LP. Outputs are restricted to the canonical set `{LP, CD, Cassette, 7", T-Shirt, Other}`. Both scripts are dry-run by default and write detailed CSV plans to `reports/finish-line/{import-plans,format-backfill}/`. Rule #8 is preserved: vinyl color variants stay separate products; sized apparel collapses to one product with multiple size variants.

- **Bandcamp apparel multi-variant create path (Phase A, 2026-04-20):** when `bandcamp-sync` encounters an unmatched merch package whose `options[]` exposes ≥2 distinct option SKUs (typically apparel sizes), the new path detected by `detectMultiVariantOptions()` (`src/trigger/lib/bandcamp-apparel.ts`) materializes EVERY option as a first-class row: one `warehouse_products` row, N `warehouse_product_variants` rows (each with `option1_name` / `option1_value` + `bandcamp_option_id` + `bandcamp_option_title` from migration `20260420000005_variant_bandcamp_option.sql`), and a single `bandcamp_product_mappings` row pointing at the first option as "primary" (the package-level unique index `uq_bandcamp_mappings_workspace_item_id` forbids per-option mapping rows). Per-option `quantity_available` is seeded through `recordInventoryChange()` with `correlationId = bandcamp-seed:{band_id}:{package_id}:opt:{option_id}`. The Shopify create uses `productOptions: [{ name: <Size|Color|Variant>, values: [...] }]` (option name inferred via `inferOptionName()` — never "Title", which would collapse the variants) and emits N variant inputs through the existing `buildShopifyVariantInput()` builder extended with `optionName`/`optionValue` parameters. After `productSetCreate` succeeds, `fetchProductVariantsByProductId()` matches Shopify variants to warehouse rows by SKU and back-fills `shopify_variant_id` + `shopify_inventory_item_id` per variant. Pre-existing umbrella apparel products (the 616 SKUs already tracked inside `bandcamp_product_mappings.bandcamp_option_skus` arrays — audit category `tracked_as_metadata`) are LEFT IN PLACE for a future Path B/C structural backfill; SKU collisions on the new create path raise a `bandcamp_apparel_sku_collision` review item rather than auto-merging.

- **Bandcamp unmatched-create ordering + containment guardrails (dupe prevention, 2026-04-19):** `bandcamp-sync` MUST create DB rows before Shopify rows on the unmatched path: create `warehouse_products` (`shopify_product_id = NULL`) → insert `warehouse_product_variants` (must capture and handle `error`) → insert `bandcamp_product_mappings` → only then call Shopify `productSetCreate` and patch `warehouse_products.shopify_product_id`. This prevents Shopify draft explosions when `warehouse_product_variants` hits `UNIQUE(workspace_id, sku)` collisions. Package-level dedup is anchored by `(workspace_id, bandcamp_item_id)` (migration `20260419000001_bandcamp_package_dedup_backstop.sql`). Additional hard guards now apply in task runtime: if `workspaces.bandcamp_sync_paused = true`, `bandcamp-sync` exits before any merch processing; unmatched create path re-validates that package mapping points to the just-created variant before calling Shopify; if post-create DB attach verification fails, the new Shopify product is immediately archived and a high-severity review queue item is opened to prevent unmanaged leakage.
- **Bandcamp OAuth queue serialization (Rule #9 — system invariant):** EVERY Trigger task that calls `refreshBandcampToken()` (directly or transitively via `getMerchDetails`, `salesReport`, `getOrders`, `updateShipped`, `updateQuantities`, `updateSku`, `getMyBands`, `getShippingOriginDetails`) MUST set `queue: bandcampQueue` (the shared `bandcamp-api` queue at `src/trigger/lib/bandcamp-queue.ts`, `concurrencyLimit: 1`). Without serialization, two concurrent token refreshes return distinct `access_token`s; the older one is invalidated by the next refresh, and the next call returns `duplicate_grant` from Bandcamp's OAuth server, which **destroys the entire token family** and requires manual re-auth in the Bandcamp dashboard. HTML-scraping tasks (`bandcamp-scrape-page`, `bandcamp-scrape-sweep`, `bandcamp-tag-backfill`) do NOT use OAuth and are exempt — they ride `bandcampScrapeQueue` / `bandcampSweepQueue`. Currently-compliant OAuth tasks: `bandcamp-sync`, `bandcamp-sync-cron`, `bandcamp-sale-poll`, `bandcamp-sales-sync`, `bandcamp-sales-backfill-cron` (Phase 0.0 hotfix 2026-04-13), `bandcamp-sales-backfill` (deprecated, defense-in-depth), `bandcamp-order-sync`, `bandcamp-order-sync-cron`, `bandcamp-mark-shipped`, `bandcamp-mark-shipped-cron`, `bandcamp-inventory-push`. Adding any new Bandcamp OAuth task without `queue: bandcampQueue` is a release-gate failure.
- **Mega-plan closeout — automated verification (Phase 6 closeout, Saturday Workstream 1, 2026-04-18):** the manual 5-SKU-per-client spot-check carried as an operator follow-up after Phase 6 is now an hourly Trigger task (`megaplan-spot-check`, `src/trigger/tasks/megaplan-spot-check.ts`). Per workspace, samples 5 SKUs daily / 15 during ramp window via the `megaplan_sample_skus_per_client` PL/pgSQL RPC (`security definer`, granted to `service_role`; ranks by recent activity in `warehouse_inventory_activity`; excludes SKUs in `count_status='count_in_progress'`). Compares DB / Redis / ShipStation v2 / Bandcamp `available`. Persists artifact to `megaplan_spot_check_runs(workspace_id, started_at, finished_at, sampled_sku_count, drift_*_count, summary_json, artifact_md)`. **Drift_major review-queue items are created ONLY when the same SKU is drift_major in two consecutive runs** (review pass v4 §5.3 — eliminates transient lag noise). Operator surface: `/admin/settings/megaplan-verification` lists recent runs, exposes a "Run spot-check now" button (calls `tasks.trigger('megaplan-spot-check')`), and renders the per-run markdown artifact in a dialog. Companion daily cron `deferred-followups-reminder` reads `docs/DEFERRED_FOLLOWUPS.md` (bundled into the Trigger build via the `additionalFiles` extension declared in `trigger.config.ts`) and upserts a `warehouse_review_queue` row per workspace for every entry whose `due_date <= today` (idempotent via `group_key='deferred-followup-{slug}'` UNIQUE per migration `20260401000003_scraper_hardening.sql`). Initial registry covers Phase 7 dormant cleanup (90 days), Tier 1 #9 (Better Stack) + #10 (statuspage.io) waivers (30 days), `external_sync_events` retention verification (1 week), ShipStation stale location cleanup (~33 days), plus four v6 codebase-verification follow-ups. Schema landed in `supabase/migrations/20260418000001_phase4b_megaplan_closeout_and_count_session.sql` — combined Workstream 1 migration that also pre-ships count-session columns on `warehouse_inventory_levels` (`count_status`, `count_started_at`, `count_started_by`, `count_baseline_available`, `has_per_location_data`) and ShipStation v2 location mirror columns on `warehouse_locations` so the WS3 features have no schema work and so the sampler RPC's `coalesce(count_status,'idle')` filter parses at function compile time. Signed signoff target: `docs/MEGA_PLAN_VERIFICATION_2026-04-13.md`.

- **Inventory count sessions + warehouse-locations source-of-truth (Saturday Workstream 3, 2026-04-18):** per-SKU **count sessions** make the existing inventory page safe for live counting. `startCountSession(sku)` flips `warehouse_inventory_levels.count_status` to `count_in_progress`, snapshots `count_baseline_available`, and records `count_started_by`. While in-progress, `setVariantLocationQuantity({ sku, locationId, quantity })` writes only to `warehouse_variant_locations` and **fanout is suppressed** — neither Bandcamp nor Shopify nor ShipStation see partial counts. `completeCountSession(sku)` re-reads current `available` (Scenario A defense — a sale that lands mid-count produces delta=0 because the live total already includes it; Scenario B count-finishes-then-sale picks the live sale up in the delta), sums per-location quantities, and routes the single resulting delta through `recordInventoryChange({ source:'cycle_count', correlationId:'count-session:{ws}:{sku}:{startedAt}' })` — Rule #20 single write path is preserved. `cancelCountSession(sku, { rollbackLocationEntries })` clears the session and optionally restores baseline. The first per-location write per SKU sets the sticky `has_per_location_data=true` flag — this is the key the deferred per-location rewrite of `shipstation-v2-sync-on-sku` (WS3 §3f) will pivot on after the §15.3 ShipStation v2 per-location semantics probe lands. **Warehouse locations are the source of truth.** `warehouse_locations` is mirrored ONE-WAY to ShipStation v2 via `createInventoryLocation` / `updateInventoryLocation` / `deleteInventoryLocation` (`src/lib/clients/shipstation-inventory-v2.ts`). Server Actions (`src/actions/locations.ts`): `createLocation` inserts the local row first then mirrors to v2 only when `workspaces.shipstation_v2_inventory_warehouse_id` is set (409 conflicts resolved via `listInventoryLocations`); `updateLocation` calls v2 FIRST on rename so a v2 failure leaves the local row unchanged and a retry has truth to retry from (v4 hardening); `createLocationRange` runs inline with throttling for ≤30 entries and offloads to the new `bulk-create-locations` Trigger task for >30 (Rule #41 keeps Server Actions bounded; the task is pinned to `shipstation` queue, respects `workspaces.shipstation_sync_paused`, surfaces partial mirror failures as one `warehouse_review_queue` row at exit); `deactivateLocation` blocks when any `warehouse_variant_locations.quantity_available > 0` references the row; `retryShipstationLocationSync` is the operator escape hatch for rows with `shipstation_sync_error`. UI: the existing `/admin/inventory` expanded-row detail now hosts `InventoryCountSessionPanel` (`src/components/admin/inventory-count-session-panel.tsx`) with Start/Complete/Cancel + per-location editable list + locator typeahead with inline `createLocation`. The standalone `/admin/inventory/locations` page is **deferred to Sunday** — inline create from the count UI covers Tue/Wed onboarding. Schema landed in the WS1 migration (no new SQL in WS3).

- **Manual inventory count entry (Saturday Workstream 2, 2026-04-18):** staff land on `/admin/inventory/manual-count` Tuesday/Wednesday to enter live on-hand counts via a per-client bulk table editor. Write semantics: **absolute set with confirmation gate** — `submitManualInventoryCounts` (`src/actions/manual-inventory-count.ts`) computes `delta = newAvailable - currentAvailable` per row, then forces a second tap via the `force:true` re-submit flow when `|delta| > 10` OR `currentAvailable === 0 && newAvailable > 0` OR `currentAvailable > 0 && newAvailable === 0`. **Negative-block contract:** any row whose `newAvailable < 0` is rejected AND a `warehouse_review_queue` row is upserted (`category='manual_count_negative_block'`, `severity='high'`, `group_key='manual-count.negative-block:{workspaceId}:{sku}'`) so an upstream sync bug is investigated rather than silently overridden. **Single-write-path discipline (Rule #20):** every applied row goes through `recordInventoryChange({ source:'manual_inventory_count', correlationId:'manual-count:{userId}:{batchId}:{sku}' })`. Bandcamp + Clandestine Shopify + client-store fanout happens automatically through `fanoutInventoryChange()` → `loadFanoutGuard()` (respects kill switches and rollout percentages). ShipStation v2 push uses a new sibling task `shipstation-v2-adjust-on-sku` (`src/trigger/tasks/shipstation-v2-adjust-on-sku.ts`) — handles BOTH directions of delta (`transaction_type:'increment'` for positive, `'decrement'` for negative), pinned to `shipstation` queue (concurrency 1), ledger-gated via `external_sync_events`. Failure to enqueue v2 sync is logged but non-fatal — Phase 5 reconcile sensor is the backstop. SKUs with `count_status='count_in_progress'` are skipped (cycle count session has priority — Workstream 3). `MAX_ENTRIES_PER_BATCH=200` bounds Server Action runtime per Rule #41.

- **Bandcamp orders → shipping margin:** `bandcamp-order-sync` writes `warehouse_orders.shipping_cost` and per-line `shipping` inside `line_items` JSON; admin shipment reads merge `customer_shipping_charged` from the order (column or inferred from `line_items`), and **on staff shipment detail** may call Bandcamp `get_orders` once to hydrate missing shipping paid when `bandcamp_payment_id` is linked, then persist—so fulfillment difference vs label cost is computable.
- **Shopify image sync invariants:**
  - `productSetCreate` MUST use `files: [FileSetInput!]` (NOT `media`) when attaching images during product creation. Shopify's `ProductSetInput` (2024-10+, including 2026-01) silently ignores the `media` field — passing it causes products to be created without images.
  - `storeScrapedImages` in `bandcamp-sync` calls `productCreateMedia` for newly-inserted Bandcamp/scraped image rows. This is the correct API for adding images after creation.
  - `upsertImagesBulk` in `shopify-sync` populates `warehouse_product_images` table during Shopify → DB delta sync. Images from Shopify are stored with `shopify_image_id` (numeric part of GID) for deduplication.
  - The `warehouse_products.images` JSONB column is a denormalised cache of the primary image URL only (for legacy compatibility). The `warehouse_product_images` table is the authoritative multi-image store.
  - The `shopify-image-backfill` task (`src/trigger/tasks/shopify-image-backfill.ts`) is a manual-trigger-only task that fixes two historical data gaps: (1) backfills `warehouse_product_images` rows from `images` JSONB for products synced before the table was properly hydrated, (2) pushes `bandcamp_art_url` via `productCreateMedia` for products with no images anywhere (`images = '[]'`).

- **ShipStation v2 inventory transaction-type semantics (Phase 0 — Patch D2 probe, 2026-04-17):** ShipStation v2 `POST /v2/inventory` enforces a `quantity >= 1` validator on every `transaction_type` that takes a `quantity`/`new_available` field — but the validator is applied asymmetrically across operations and lifecycle stages. Empirically verified contract (full evidence: plan §5.1, §7.1.6.0, `reports/shipstation-v2-decrement-to-zero-2026-04-17.json`):
  - `increment quantity: 0` on a fresh SKU → **400** (seed cannot create a zero-stock row).
  - `increment quantity: 1+` on a fresh SKU → **200** (canonical seed path).
  - `decrement quantity: N` on tracked SKU, including landing on `available: 0` → **200**. Use this for the natural delta path; **fanout MUST use `decrement` at the 1 → 0 boundary**, no special branch.
  - `adjust quantity: 0` on tracked SKU → **200** (asymmetric vs seed; this is the documented safety net if `decrement` ever fails).
  - `modify new_available: 0` → **400** (`modify` cannot zero a SKU). **Forbidden** in any inventory write path; Phase 4 fanout unit tests must assert this combination is never emitted.
  - When tracked `available` reaches 0, ShipStation drops the row from `GET /v2/inventory?sku=…` (returns empty `inventory: []`). Reconcile (Phase 5) MUST treat "DB has `available: 0`" and "ShipStation returns no row" as equal, not as drift.

- **ShipStation v1 product PUT is full-resource replacement (Phase 0.5 — 2026-04-17):** `PUT /products/{productId}` on the v1 API replaces the entire product document — any field omitted from the payload is reset on the server. This makes the `GET → merge → PUT` cycle a concurrency hazard: two concurrent rectifiers reading the same pre-image and writing different merged payloads will lose one another's changes. **Mandatory pattern:** every v1 product write must execute inside a per-`shipstation_product_id` Redis mutex (`src/trigger/lib/redis-mutex.ts`, `DEFAULT_MUTEX_TTL_SEC = 120` — strictly exceeds the v1 `Retry-After` ceiling so a 429 sleep cannot expire the lock under another task's feet). Mutex release is gated by a Lua `GET == token → DEL` script; never `DEL` a key blindly. Every v1 product mutation must capture a `pre_image` to `sku_remap_history` and verify post-write state before declaring success. The canonical wrapper is `addAliasToProduct` / `removeAliasFromProduct` in `src/lib/clients/shipstation.ts` (idempotent merge on `aliases[]`).
- **External sync ledger idempotency (Phase 0.5 — 2026-04-17):** every external mutation (`shipstation_v1`, `shipstation_v2`, `bandcamp`, `clandestine_shopify`) MUST be bracketed by `beginExternalSync` / `markExternalSyncSuccess|markExternalSyncError` in `src/lib/server/external-sync-events.ts`. The `external_sync_events` table enforces `UNIQUE(system, correlation_id, sku, action)` and `beginExternalSync` returns the existing row on `23505` so retries are no-ops. Direct external API writes that bypass this ledger lose distributed idempotency and are a release-gate failure.
- **SKU rectify is suggest-don't-mutate (Phase 0.5 — 2026-04-17):** the `sku-sync-audit` cron and any other detector MUST only write to `sku_sync_conflicts` (keyed by deterministic `group_key`, bumping `occurrence_count` on re-detect, reopening `resolved` rows). Mutation across systems happens only through `sku-rectify-via-alias` (staff-approved via `applyAliasResolution` Server Action) using ShipStation v1 aliases. Detectors must never call `addAliasToProduct`, `putProduct`, or any external write directly.
- **Bandcamp push_mode contract (Phase 1 — 2026-04-17):** `bandcamp_product_mappings.push_mode` (`bandcamp_push_mode` enum: `normal | blocked_baseline | blocked_multi_origin | manual_override`) gates every Bandcamp write. `bandcamp-inventory-push` MUST filter mappings to `push_mode IN ('normal','manual_override')` at the source — `blocked_*` rows are dropped before any `update_quantities` call. `manual_override` is operator opt-in (e.g. preorder window, recall) and is **NEVER auto-cleared** by the audit; only staff can flip it via `setBandcampPushMode`. The audit (`bandcamp-baseline-audit`, `0 3 * * *`, `bandcampQueue`) has two passes: (1) per-mapping baseline detection from already-stored `raw_api_data` + `bandcamp_origin_quantities` (no API call) — `baseline_inferred = top - sum(origin_quantities[].option_quantities[].quantity_available)`; if `> 0` upsert `bandcamp_baseline_anomalies` and set `push_mode='blocked_baseline'`; if `= 0` and currently `blocked_baseline`, flip to `normal` and resolve open anomalies; (2) per-band multi-origin probe via `getShippingOriginDetails` — if `> 1` origin, set every mapping for that band to `blocked_multi_origin` (only from `normal`); if `≤ 1` clear `blocked_multi_origin` rows back to `normal`. Suggest-don't-mutate: the audit NEVER calls `update_quantities` or `update_sku`; it only writes anomaly rows + `push_mode*` audit columns. Staff-driven overrides (`forceBaselineScan`, `setBandcampPushMode`) live in `src/actions/bandcamp-baseline.ts` and never call Bandcamp directly — they always enqueue via `tasks.trigger('bandcamp-baseline-audit')` (Rule #48).
- **Bundle availability has a single shared helper (Phase 2.5(b) — 2026-04-13):** `computeBundleAvailability(components, componentInventory)` and `computeEffectiveBundleAvailable(bundleStockOnHand, components, componentInventory)` in `src/lib/server/bundles.ts` are the canonical bundle math. EVERY fanout site that pushes a bundle SKU's quantity downstream MUST consult `computeEffectiveBundleAvailable` — never reimplement the `MIN(bundle_stock, MIN(component_available / per_unit_qty))` formula inline. Currently wired in `bandcamp-inventory-push.ts`; when Phase 4 adds ShipStation v2 fanout to the inventory chain, that fanout will use the same helper. Empty `components` array returns `+Infinity` so callers can chain naturally with `Math.min(bundleStock, …)`. Per-unit `quantity <= 0` is normalized to `1` (defensive — a misconfigured bundle row never propagates a divide-by-zero crash).
- **Bundle derived-drift sensor (Phase 2.5(c) — 2026-04-13):** `bundle-derived-drift-sensor-schedule` (cron `0 * * * *`, `shipstationQueue`) compares ShipStation v2's stored `available` for each bundle SKU against the value derived via `computeEffectiveBundleAvailable`. Bundle SKUs are deliberately NOT seeded into v2 (Phase 2.5(a)); the sensor catches the case where v2 has a row anyway (merchant manual edit, pre-existing import, future fanout side effect). Drift > `BUNDLE_DRIFT_TOLERANCE` (default 2 units) opens a `warehouse_review_queue` row keyed on `group_key='bundle.derived_drift:{workspace_id}:{sku}'` (Rule #55 dedupe — re-detection bumps `occurrence_count` on the existing open row instead of fanning out duplicates). Bundle SKUs that v2 does not return are silently skipped — the (a) exclusion is the desired state, not a failure mode. The sensor never writes to v2 inventory or to `warehouse_inventory_levels`; it is purely a detector and is therefore exempt from the `recordInventoryChange` write path (Rule #20). The `runBundleDerivedDriftSensor` function takes injectable `inventoryFetcher` + `getWorkspaceIds` + `supabase` deps so it is unit-testable without the v2 HTTP surface.
- **Bandcamp warehouse seed uses origin sum, not TOP (Phase 1 follow-up #2 — 2026-04-13):** Both seed sites in `bandcamp-sync.ts` (the existing-mapping seed at the `bandcamp_initial` authority-status branch and the brand-new-variant seed inside the variant-creation block) MUST consult `computeBandcampSeedQuantity(merchItem)` from `src/lib/server/bandcamp-effective-available.ts` and use its `effective` value as the `recordInventoryChange` delta — never `merchItem.quantity_available` (TOP) directly. Decision tree: if `origin_quantities` is a non-empty array, trust the origin sum (even if it is 0 — that is the baseline-anomaly case Phase 1's audit catches and flips `push_mode='blocked_baseline'` for); else if TOP > 0 fall back to TOP and record `effective_source='top_fallback'` in metadata so operators can grep `warehouse_inventory_activity` for merchants who have not backfilled origin metadata; else seed nothing. This is the closure for Phase 1 follow-up #2 in the mega-plan, the hard prerequisite for Phase 4's bidirectional bridge: without this fix, a SHIP_NOTIFY shipment of an inflated-warehouse product would push the inflated value back to Bandcamp's origin allocation via `update_quantities` and double-display to customers (`new_top = baseline + inflated_origin`). The `effective_source` enum (`origin_sum | top_fallback | zero`) is the single source of truth for downstream auditing of seed quality.
- **Distro discriminator (Phase 0.7 — 2026-04-17):** `warehouse_products.org_id` is **nullable**. `org_id IS NULL` is the canonical signal that a product is "distro" — owned by Clandestine, no fulfillment client, no Bandcamp upstream. The discriminator is enforced at three points: (1) migration `20260417000003_distro_dormancy.sql` drops the `NOT NULL`, adds a partial index `idx_warehouse_products_distro WHERE org_id IS NULL`, and comments the column; (2) `clandestine-shopify-sync` (daily 04:30 UTC, after `bandcamp-baseline-audit`) is the ONLY task allowed to insert distro rows — it skips products that already have a `warehouse_products` row and skips products whose variant SKUs appear in `bandcamp_product_mappings` (those belong to client `bandcamp-sync`); (3) `bandcamp-inventory-push` defensively filters out distro variants via the `warehouse_product_variants!inner(warehouse_products!inner(org_id))` join, logging skips per connection. RLS on `warehouse_products` already hides `org_id IS NULL` rows from clients (`org_id = get_user_org_id()` is FALSE for NULL); staff and service_role see them. Inventory writes for distro items still flow through `recordInventoryChange()` (Rule #20) and arrive via SHIP_NOTIFY (Phase 2) — `clandestine-shopify-sync` is catalog-only.
- **ShipStation v2 inventory client is batch-only (Phase 2 — 2026-04-13):** `src/lib/clients/shipstation-inventory-v2.ts` exports `listInventory({ skus?, … })` (batch read, auto-chunked at `V2_INVENTORY_LIST_BATCH_LIMIT = 50` SKUs/request, cursor-paged when `skus` is omitted) and `adjustInventoryV2()` (single-SKU write, allowed). It deliberately does NOT export a single-SKU read helper such as `getInventoryBySku(sku)`. Callers that need one record do `const [r] = await listInventory({ skus: [sku] })` — the same one HTTP request, but it forces every read site through the batch path so future call-sites can add to the batch instead of reinventing a per-SKU helper that quietly burns the v2 60 req/min budget. Enforcement: CI guard `scripts/check-v2-inventory-batch.sh` (wired into `scripts/release-gate.sh`) greps the source tree for forbidden symbol shapes (`getInventoryBySku`, `findInventoryBySku`, `inventoryFor(sku:`) outside the v2 client itself and its tests; any reintroduction is a release-gate failure. The runtime client also enforces the Phase 0 Patch D2 boundary contract client-side: `modify new_available: 0` and `decrement quantity: 0` are rejected before the HTTP call; `adjust quantity: 0` is allowed (asymmetric safety net).
- **SHIP_NOTIFY is the inventory ingress for shipped items (Phase 2 — 2026-04-13):** `/api/webhooks/shipstation` receives ShipStation `SHIP_NOTIFY` events, verifies `x-ss-signature` against `SHIPSTATION_WEBHOOK_SECRET` on the raw body (Rule #36/#63), dedupes via `webhook_events` keyed on `external_webhook_id = 'shipstation:ship_notify:{resource_url}'` (Rule #62), enqueues `process-shipstation-shipment`, and returns 200 OK in <500ms (Rule #66). The processor task is the **only** place a SHIP_NOTIFY-driven inventory write happens. It pulls the shipment(s) via the v1 `resource_url` (validated host = `ssapi.shipstation.com`, `includeShipmentItems=True` appended), skips voided shipments, resolves the org via `matchShipmentOrg()` (3-tier: store mapping → ShipStation alias match → SKU match; unresolvable → review queue `severity:high`, **no inventory writes** for the shipment — failing closed), then per line item resolves the SKU via `findVariantBySkuOrAlias()` (3-tier: direct `warehouse_product_variants.sku` (org-scoped or distro `org_id IS NULL`) → `client_store_sku_mappings.remote_sku` for client-store aliases → `sku_remap_history.from_sku → to_sku` for DB-side renames). Unknown SKUs land in the review queue at `severity:medium` and the rest of the shipment continues — per-line failure isolation. Inventory decrement is the canonical write path: `recordInventoryChange({ source: 'shipstation', delta: -qty, correlationId: 'ssv1:shipment:{shipmentId}:{sku}', metadata: { source_subtype: 'ship_notify', shipment_id, order_number, line_item_key, tracking_number, original_sku, resolved_from_alias } })`. The Redis `SETNX processed_wh:{key}` guard (Rule #47) and the `warehouse_inventory_activity (sku, correlation_id)` UNIQUE constraint inside `record_inventory_change_txn` make re-fired SHIP_NOTIFYs for the same line item idempotent on both Redis and Postgres. Bandcamp / client-store fanout is an automatic side effect of `recordInventoryChange` (Rule #48 — never call APIs directly from the processor). Outbound ShipStation v2 mutations triggered by the fanout chain MUST go through `external_sync_events` ledger brackets and `adjustInventoryV2`.
- **ShipStation v2 inventory seed is one-shot, gated, and ledger-idempotent (Phase 3 — 2026-04-13):** `shipstation-seed-inventory` is the canonical bootstrap path that flips a workspace from "Bandcamp-as-truth" to "ShipStation-as-truth" for the SKUs Clandestine actually fulfills. Selection cascade (per plan §7.1.5, enforced in `src/trigger/tasks/shipstation-seed-inventory.ts`): variant has `org_id IS NOT NULL` (fulfillment client only — distro `org_id IS NULL` is NEVER seeded into v2) → variant is NOT a `bundle_components.bundle_variant_id` (Phase 2.5(a) bundle exclusion — bundles derive availability from components, not from a standalone v2 row) → `bandcamp_product_mappings.push_mode = 'normal'` (excludes `blocked_baseline`, `blocked_multi_origin`, `manual_override`) → `computeEffectiveBandcampAvailable(bandcamp_origin_quantities) > 0` (the helper in `src/lib/server/bandcamp-effective-available.ts` sums the actual controlled origin quantities, NOT Bandcamp's customer-facing `quantity_available` which can be inflated by merchant-set baselines — Part 9 audit) → `warehouse_inventory_levels.available > 0`. Push value = `warehouse_inventory_levels.available` via `adjustInventoryV2({ transaction_type:'increment' })` per SKU. Idempotency is enforced by the `external_sync_events` ledger (`UNIQUE(system, correlation_id, sku, action)`) keyed by `(system='shipstation_v2', correlation_id='seed:{workspace_id}:{run_id}', sku, action='increment')` — Trigger.dev replays reuse the same `ctx.run.id` so duplicate inserts short-circuit and the SKU is counted as `ledger_skipped`. The task is pinned to `shipstationQueue` (concurrencyLimit 1) — same queue as `process-shipstation-shipment` so seed bursts and real-time SHIP_NOTIFY processing serialize against ShipStation v2's 60 req/min budget. `dryRun: true` evaluates the full cascade against Postgres without calling v2, surfacing gate counts to the admin Preview button. The task is bulk-shaped (Rule #59) and does NOT route through `recordInventoryChange()`; the Phase 5 reconcile sensor is the safety net for any drift introduced during seeding. Server Actions live in `src/actions/shipstation-seed.ts` and enforce `requireAuth` + `isStaff` + `workspaceId == userRecord.workspace_id`; preview polls inline up to 25s before returning a pending status (Rule #41); the real seed returns the task run id immediately and the UI polls `channel_sync_log` (`channel='shipstation_v2'`, `sync_type='seed_inventory'`) for progress (Rule #48 — never call ShipStation directly). Companion stub `shipstation-store-refresh` records refresh requests to `channel_sync_log` with `status:'deferred'` while Open Question #2 (public refresh endpoint existence) is unresolved — the day-one race fallback is the 24h auto-import + Phase 5 reconcile.
- **Direct-Shopify cutover finish-line invariants (2026-04-22):** Three hard contracts gate any future change near the Shopify cutover surface — see `docs/RELEASE_GATE_CRITERIA.md` Section C.1 for the corresponding cutover gates.
  - **(a) Partial-cancel recredit honors `warehouse_order_items.fulfilled_quantity` (F-1 / HRD-08.1).** `handleOrderCreated` writes per-line `fulfilled_quantity` from `line_items[i].fulfillment_status === 'fulfilled'` so `handleOrderCancelled` can compute the remaining-unfulfilled units and recredit ONLY that delta. The DB row is canonical when it disagrees with the inbound `orders/cancelled` payload — a `db_webhook_disagree` flag is emitted on the cancel telemetry row. **Design intent (Q2 confirmed 2026-04-22):** when a cancel arrives with mixed fulfillment_status we choose **option (a) — skip recredit on fulfilled lines entirely** (`recreditResults.status='skipped_already_fulfilled'`, zero `recordInventoryChange` call), NOT option (b) "credit-all and rely on inventory reconciliation to catch shrinkage". Option (a) preserves the principle that already-shipped units leave the warehouse permanently — the customer-facing refund handles money, and the megaplan spot-check sensor catches any genuine shrinkage. Adopting option (b) would silently re-credit the shipped units back into salable inventory and produce phantom oversells. **Source-of-truth invariant: `fulfilled_quantity` is write-only from webhook handlers.** Server Actions, admin tools, and backfill scripts must NEVER mutate it. Enforced by `scripts/check-fulfilled-quantity-writers.sh` (CI grep guard).
  - **(b) Every webhook Route Handler is pinned to Node + force-dynamic (F-2 / HRD-23).** Edge runtime lacks `node:crypto` and a stable Supabase service-role client; static caching of webhook responses would corrupt the dedup invariant (Rule #62). Every `src/app/api/webhooks/**/route.ts` MUST export both `runtime = "nodejs"` and `dynamic = "force-dynamic"`. Enforced by `scripts/check-webhook-runtime.sh` with a deliberately-empty `WEBHOOK_RUNTIME_ALLOWLIST` escape hatch.
  - **(c) Shopify install verifies `shop.myshopifyDomain` before token persistence (F-5 / HRD-10).** The OAuth callback at `src/app/api/oauth/shopify/route.ts` queries `shop { myshopifyDomain }` immediately after the code-exchange and rejects the install if the canonical domain doesn't match the normalized `shop` query param (lowercase + `.myshopify.com` suffix + trailing `/` stripped). On mismatch a `warehouse_review_queue` row is upserted (`category='security'`, `severity='high'`, `group_key='shop_token_mismatch:{org_id}:{shop}'`) — even benign mismatches deserve visibility because repeats indicate a token-reuse attack pattern. The verified domain is persisted to `client_store_connections.shopify_verified_domain` so re-checks compare against truth without re-querying Shopify.

- **Webhook event dedup is typed + structured (F-3 / F-4, 2026-04-22):** `src/lib/server/webhook-body.ts` exports `interpretDedupError(err)` returning a `DedupResult` discriminant — `fresh | duplicate | transient | unknown`. The Route Handler at `src/app/api/webhooks/client-store/route.ts` maps `transient` (PG `08006`/`08001` etc.) to HTTP 503 (so upstream retries) and `unknown` to 503 + Sentry; `duplicate` returns 200 OK without re-processing. Every non-`fresh` outcome logs structured context: `{ connection_id, platform, topic, external_webhook_id, dedup_kind, error_code }`. Squarespace-style platforms with no header-derived event id use `canonicalBodyDedupKey({ platform, rawBody })` → `{platform}:{sha256(rawBody)}` as the fallback key. The resolved key is persisted on `webhook_events.dedup_key` so operators can grep by the canonical value when investigating duplicate-rejection traces.

- **Bandcamp order sync is rate-budget-aware (B-1, 2026-04-22):** `bandcamp-order-sync-cron` runs at `*/15 * * * *` (tightened from `0 */6 * * *`) on the shared `bandcampQueue` (Rule #9). The schedule definition uses `idempotencyKeys.create('bandcamp-order-sync-cron', { scope: 'global' })` + `idempotencyKeyTTL: '15m'` so overlapping ticks collapse to a single in-flight run instead of stacking. When a tick is deduped against an in-flight run, a `sensor_readings` row (`sensor_name='bandcamp.cron_idem_skip'`, `value: { reason:'overlapping_run', last_run_id, this_attempt_at }`) is emitted per workspace so cron flapping is visible — silent flapping would mask real failures. Per-order idempotency inside the loop is keyed `bandcamp:order-sync:{band_id}:{order_id}` — derived ONLY from Bandcamp identifiers (`payment_id`); a clock-driven key would defeat retry semantics. Cadence budget header in the source documents the ~120 req/run × 96 runs/day envelope; if the workspace count grows past 15 the cadence must be revisited (`*/30` or per-workspace stagger).

- **Shopify fulfillment uses GraphQL `fulfillmentCreate` (B-2 / HRD-28, 2026-04-22):** `mark-platform-fulfilled` no longer calls Shopify REST. The flow lives in `src/lib/server/shopify-fulfillment.ts` (pure functions) + the task wrapper. Step 1 fetches all fulfillment orders for the order GID; Step 2 (`selectFulfillmentOrder`) filters to `status IN ('OPEN', 'IN_PROGRESS')` and picks the FO whose `lineItems[].sku` set covers the warehouse order's required SKUs (derived from `warehouse_order_items.fulfilled_quantity`). Tie-breaker on ambiguous coverage: oldest GID + a `sensor_readings` warning emitted so operators see the noise level. Step 3 (`runFulfillmentCreateMutation`) treats BOTH a non-empty top-level `errors[]` AND `data.fulfillmentCreate.userErrors[]` as failure (Shopify returns partial data when userErrors fire). On failure the `ShopifyFulfillmentError` carries the raw `errors`/`userErrors` payload + `partialFulfillmentId` to `warehouse_review_queue.metadata` for forensics — the platform side is NOT marked fulfilled in this case. Per CLAUDE.md Rule #1: this is a `fulfillmentCreate` mutation (not `productSet`) so the full-shape builder rule does not apply.

- **Channels page surfaces per-connection webhook health + idempotent re-register (B-3 / HRD-14, 2026-04-22):** `client_store_connections.last_webhook_at` is the SOLE freshness clock for the Channels webhook health card — it is updated by `process-client-store-webhook` on every successful event completion (processed OR echo_cancelled). The card classifies state via 1h / 6h thresholds (`healthy / delayed / stale / unknown`). `webhook_events.last_seen_at` (HRD-01 monotonic guard) is intentionally NOT joined into the health card — having two freshness fields invites drift. Per-required-topic counters live in `client_store_connections.webhook_topic_health` JSONB (snapshot, not history; overwritten on each successful event). The "Re-register webhooks" button calls `registerShopifyWebhookSubscriptions` (idempotent — existing matching subscriptions are reused, only missing topics are created). The pure helper `diffWebhookSubscriptions(current, desiredCallbackUrl)` in `src/lib/server/shopify-webhook-subscriptions.ts` returns `{ toCreate, toRecreate, toDelete, inSync }` and is the canonical "what would change" surface for both the UI preview and any future `shopify-webhook-health-check` cron task.

- **Megaplan spot-check is a 5-source comparison with symmetric classification (B-4 / HRD-15, 2026-04-22):** `megaplan-spot-check` adds Shopify-direct as a 5th source alongside DB / Redis / ShipStation v2 / Bandcamp. Per-connection batching (`MAX_SHOPIFY_DIRECT_PROBES_PER_CONNECTION = 50`) keeps the GraphQL `inventoryLevels` cost inside Shopify's per-second budget; high-SKU stores rotate which subset is sampled across runs. Symmetric classification adds two new states alongside the existing four: `legacy_drift` (Shopify+DB agree, ShipStation disagrees — informational artifact only, NOT escalated to the review queue; tells us where SS v2 truth was wrong before cutover) and `bandcamp_drift` (Shopify+DB+SS agree, Bandcamp disagrees). `drift_major` escalates immediately on a Shopify-direct mismatch >2. Persistence rule (review pass v4 §5.3) still applies — only `drift_major` SKUs that repeat in two consecutive runs create a `warehouse_review_queue` item. The aggregate count of successfully-probed SKUs lands on `megaplan_spot_check_runs.shopify_direct_available`; per-SKU values live in `summary_json.rows[].shopifyDirectAvailable`.

- **Per-client Shopify Custom-distribution app onboarding (HRD-35, 2026-04-22):** Direct-Shopify cutover does NOT use a single Clandestine-owned public Partner app. Each client store is paired with its own **Custom-distribution Partner app** created inside that client's Shopify Partner organization (Shopify hard-rejects installs across Partner orgs for Custom-distribution apps — verified during Northern Spy fast-path probe). Per-connection app credentials live on `client_store_connections.shopify_app_client_id` + `shopify_app_client_secret_encrypted` (plaintext today; column name forward-compatible with the deferred encryption-at-rest work `client-store-credentials-at-rest-encryption`). The OAuth callback at `src/app/api/oauth/shopify/route.ts` resolves credentials per-connection (`resolveAppCredentials(connectionId)`) and falls back to `SHOPIFY_CLIENT_ID` / `SHOPIFY_CLIENT_SECRET` env vars only when no per-connection app is configured (legacy single-app path; HRD-35 requires per-connection going forward). State-nonce protection (HRD-35.1) is enforced via the `oauth_states` table (Section F of migration `20260422000001_direct_shopify_metadata.sql`): the install endpoint inserts a row keyed `(oauth_token=nonce, nonce_purpose='shopify_install', org_id, connection_id, expires_at = now() + 15min)` and the callback does a single-use SELECT-then-DELETE plus `expires_at` and `org_id` cross-checks before honoring the code exchange. Newly-installed connections land with `do_not_fanout = true` (Phase 0.8 dormancy default — applies to BOTH per-connection and env-fallback paths); staff explicitly call `reactivateClientStoreConnection` after pairing SKUs (`autoDiscoverSkus`) and selecting a default Shopify location (`setShopifyDefaultLocation`). Onboarding Server Actions live in `src/actions/store-connections.ts`: `setShopifyAppCredentials` (paste app creds), `generateShopifyInstallUrl` (build install link with `connection_id` encoded in state), `listShopifyLocations` (per-connection token, `read_locations` scope required — installs predating the HRD-25 scope expansion need re-consent), `setShopifyDefaultLocation` (defense-in-depth: re-verifies the locationId is in the live Shopify list AND active before persisting). UI: per-connection "Configure App" dialog at `/admin/settings/store-connections` (`src/components/admin/configure-shopify-app-dialog.tsx`) walks staff through the three steps with explicit step badges. Northern Spy probe (2026-04-21) verified the end-to-end OAuth flow against `2b65b8-2.myshopify.com` — the live token hit `read_products` / `read_inventory` / `read_orders` cleanly and the `read_locations` 403 confirmed scope enforcement.
- **Customer tracking + notification hardening (tracking-notification v5, 2026-04-25):** End-to-end hardening of the EasyPost-driven customer tracking page and Resend-delivered shipment emails. Four sequential slices captured in `docs/RELEASE_SLICES_TRACKING_NOTIFICATION.md`. (1) **Webhook security + provider ledger:** `/api/webhooks/easypost` (new) verifies HMAC v1+v2 signatures via `verifyEasyPostSignature` (`src/lib/server/easypost-webhook-signature.ts`) with dual-secret rotation (`EASYPOST_WEBHOOK_SECRET` + `EASYPOST_WEBHOOK_SECRET_PREVIOUS`); production fails closed when secret is unset; signature failures append a `signature_failed` row to `webhook_events` for forensic audit; dedup uses `interpretDedupError` (duplicate→200, transient→503, unknown→503+Sentry). `/api/webhooks/resend` migrated to the same dual-secret + `interpretDedupError` posture. New tables: `notification_provider_events` (append-only ledger of every provider event with `workspace_id` + `shipment_id` normalized columns for fast drilldown — Slice 1 schema; later extended for `easypost` provider in Slice 1b), `webhook_events.status` enriched with `signature_failed`/`enqueue_failed`. (2) **Notification idempotency + state machine:** `notification_sends` has a partial unique index on `(workspace_id, shipment_id, kind, idempotency_key)`; preflight script `pnpm ops:check-webhook-secrets` plus a duplicate-row detector run before the migration widens the index (fails loudly on conflict instead of silently coalescing rows). Status enum widened to include `pending` / `sent` / `delivered` / `bounced` / `complained` / `provider_suppressed` / `provider_failed` / `delayed` / `cancelled` / `shadow` / `ignored`. Sticky terminal state machine via PL/pgSQL `update_notification_status_safe` + `update_shipment_tracking_status_safe` (out-of-order events and terminal-overwrites are rejected and reported back as `skipped_reason`). All status writes route through `src/lib/server/notification-status.ts`; CI grep guard `scripts/check-notification-status-writes.sh` fails the build on direct writes to `notification_sends.status` or `warehouse_shipments.easypost_tracker_status` outside the wrapper. Resend send path uses Resend SDK `idempotencyKey` (Slice 2 idempotency triple-defense alongside the unique index and `provider_event_id` dedup). `notification_operator_events` table (FK → `public.users`) audits every operator retry/cancel via `apply_operator_notification_action` RPC. `send-tracking-email` reordered to insert the `pending` `notification_sends` row BEFORE the Resend call so a process crash between insert and send is recovered by `notification-failure-sensor` rather than silently dropping the email. (3) **Tracking persistence + public page hardening:** `easypost-register-tracker` Trigger task (replaces `aftership-register` for the canonical path; aftership remains as legacy until the parity sensor `tracking.status_drift_24h` is green for 30 days) persists `easypost_tracker_id` + `easypost_tracker_status` to `warehouse_shipments` via the safe wrapper. `warehouse_tracking_events.provider_event_id` deduplicates EasyPost tracker events. Public tracking page (`/track/[token]`) and email template are PII-allowlisted via `pickPublicDestination` (only `name`, `city`, `state`, `country`, `postal_code` — never `street1`/`street2`/`phone`/`email`); brand colors run through `sanitizeBrandColor`; image URLs through `sanitizeImageUrl`; `PublicTrackingShipment` types enforce the allowlist at the type system. Carrier deeplinks generated via `buildCarrierTrackingUrl` (no third-party redirect risk). (4) **Ops visibility:** `/admin/operations/notifications` page surfaces 24h notification status counts, suppressed recipients, last sensor run, and currently-stuck rows. Per-shipment audit drilldown (`getShipmentNotificationLog`) joins `notification_sends` + `notification_provider_events` (normalized `shipment_id` column, NOT JSON-path) + `notification_operator_events` for the full lifecycle of every customer email tied to a shipment; linked from every admin shipment surface (Shipping log, Order detail, Operations notifications) plus a "View as customer" link to the `/track/[token]` page. Operator actions: `retryStuckNotification` / `cancelStuckNotification` route through `applyOperatorNotificationAction`; `triggerNotificationFailureSensor` is a manual button for incident response. Sensors in `unified-shipping-sensors.ts` (`notification.send_failure_rate_24h`, `resend.bounce_rate_24h`, `resend.complaint_rate_24h`) filter by `created_at` (not `sent_at`, which is NULL for failed sends) and count both legacy (`sent`/`failed`) and new (`delivered`/`provider_failed`) statuses for backward compatibility during the rollout window. Test coverage (194 unit/integration tests passing): webhook signature verification (EP v1+v2, Resend Svix), route handlers (EP, Resend incl. `email.suppressed` mapping), provider event idempotency, send helpers (thenable mocked Supabase chains), notification status wrapper, operator actions, public tracking page render with PII allowlist, CI guards (`check-webhook-secrets`, `check-notification-status-writes`).
- **WooCommerce + Squarespace catalog hardening (2026-04-24):** `src/actions/store-connections.ts::testStoreConnection()` and the legacy non-Shopify `autoDiscoverSkus()` path now validate against real remote catalogs instead of shallow probes. `src/lib/clients/woocommerce-client.ts` is the single owner of Woo catalog traversal: it caps `per_page` at 20 for stability, paginates `/products`, walks `/products/{id}/variations` for variable-product SKUs, and supports absolute stock writes to either product or variation endpoints. `src/lib/clients/squarespace-client.ts` now uses the official `https://api.squarespace.com` host for inventory/orders/products, paginates inventory reads, and materializes variant identity through `v2/commerce/products` plus `getProductsByIds()`. Operational effect: WooCommerce and Squarespace connections can be tested and auto-discovered against full catalogs, which makes dormant-connection verification and future product-identity matching materially more reliable.
- **Client store dormancy gate (Phase 0.8 — 2026-04-17):** `client_store_connections.do_not_fanout` is **TRUE by default for every Shopify / WooCommerce / Squarespace connection** (set en masse by migration `20260417000003_distro_dormancy.sql`). ShipStation Inventory Sync is the canonical fanout path for client storefronts; first-party connectors stay in the codebase as backups but are dormant unless staff explicitly reactivate them. The single chokepoint is `shouldFanoutToConnection(connection)` in `src/lib/server/client-store-fanout-gate.ts`. Every code path that would push to or pull from a client store MUST consult the gate first: `multi-store-inventory-push`, `client-store-order-detect`, `process-client-store-webhook`, and the `createStoreSyncClient` constructor (last-line `DormantConnectionError` defense). Webhooks from dormant connections are recorded with `webhook_events.status = 'dormant_skipped'` so we have an audit trail. Reactivation goes through `reactivateClientStoreConnection({ connectionId })` (staff-only Server Action in `src/actions/store-connections.ts`); the admin UI is `/admin/settings/client-store-reconnect`. Portal "Connected Stores" is hidden (sidebar nav, `TOGGLEABLE_PAGES`, settings card, home cards) and `/portal/stores` redirects to `/portal` via middleware. CI guard: `scripts/check-fanout-gate.sh` greps for `client_store_connections` reads paired with fanout sinks (`createStoreSyncClient`, `client_store_credentials`) and fails the build if `shouldFanoutToConnection` is missing or the file is not on the audited allow-list.
- **Rollout audit + auto-halt sensor (Phase 6 finish-line v4 — 2026-04-13):** Every change to `workspaces.fanout_rollout_percent` flows through `setFanoutRolloutPercentInternal` in `src/lib/server/admin-rollout-internal.ts`, which appends to the new `workspaces.fanout_rollout_audit jsonb NOT NULL DEFAULT '[]'` column. Audit row shape: `{ ts, percent_before, percent_after, reason, actor:{kind:'staff'|'sensor'|'script'}, sensor_run? }`. The staff entry point is the Server Action `setFanoutRolloutPercent` in `src/actions/admin-rollout.ts` (auth-gated via `requireStaff()`); the auto-halt entry point is the cron task `ramp-halt-criteria-sensor` in `src/trigger/tasks/ramp-halt-criteria-sensor.ts` (cron `*/2 * * * *` during ramp, tighten to `*/15` post-ramp per Phase 8e). The sensor MUST NOT call the Server Action — Trigger tasks lack a Supabase auth session — so it calls `setFanoutRolloutPercentInternal` directly with `actor.kind = 'sensor'` and `sensor_run = ctx.run.id`. Halt criteria (§31, refined by finish-line v4 §5.3) live in the pure evaluator `src/trigger/lib/ramp-halt-evaluator.ts`: H-1 `inv.redis_postgres_drift` critical, H-2 `inv.propagation_lag` critical, H-3 spot-check `drift_major > 5%` (TWO consecutive runs required — single-run flap is warn-only), H-4 ShipStation v2 5xx > 2% (escalates to `halt_and_page`), H-5 `webhook.silence` (warn-only unless paired with H-1). §5.3 two-consecutive-runs persistence lives in the new `workspaces.ramp_sensor_state jsonb` column (`{ lastSpotCheckTripped, lastEvaluatedAt, lastSensorRun }`). The sensor filters STRESS-* / `metadata->>'stress_run_id'` rows out of inputs so harness traffic never trips production halt criteria. Rollout helper has 22 unit tests; admin UI page `/admin/settings/megaplan-ramp` is deferred (slug `megaplan-ramp-admin-page`).
- **Per-integration kill switches and percentage rollouts (Tier 1 hardening #1, #13 — 2026-04-13):** `workspaces` carries five guard columns: the existing global `inventory_sync_paused`, plus per-integration `shipstation_sync_paused`, `bandcamp_sync_paused`, `clandestine_shopify_sync_paused`, `client_store_sync_paused` (each with companion `*_paused_at` / `*_paused_by` audit columns), and `fanout_rollout_percent smallint NOT NULL DEFAULT 100 CHECK (BETWEEN 0 AND 100)`. The single chokepoint is `loadFanoutGuard(supabase, workspaceId)` + `guard.shouldFanout(integration, correlationId)` in `src/lib/server/fanout-guard.ts`. `inventory-fanout.ts` (the central inventory fanout entry point) MUST consult the guard before each integration push (Clandestine Shopify, client stores, Bandcamp, ShipStation v2 fanout when wired in Phase 4). Rollout bucketing is deterministic: `correlationIdBucket(correlationId)` uses 32-bit FNV-1a so a given correlation_id always lands in the same 0..99 bucket — replays with the same `webhook_id` / `task_run_id` re-evaluate identically and never half-emit the same event. `fanout_rollout_percent = 100` is a no-op (everything passes); `0` halts everything; values in between gate by `bucket < percent`. Global pause is checked first; integration-specific pause next; rollout last. Operator workflow: flip a single column to pause one integration without halting the rest of the system, or set `fanout_rollout_percent = 10` to canary a new fanout pipeline against 10% of traffic before flipping back to 100. Guard caches per-call only — no in-process memoization, so a workspaces-row update takes effect on the next fanout invocation.
- **External sync events retention is automated (Tier 1 hardening #14 — 2026-04-13):** `external-sync-events-retention` (cron `30 3 * * *` America/New_York) prunes `external_sync_events` to bound table growth: rows with `status='success'` and `completed_at < now() - interval '7 days'` are deleted; rows with `status='error'` are kept 30 days for forensics. The cleanup is backed by partial index `idx_external_sync_events_completed_status (status, completed_at) WHERE completed_at IS NOT NULL` (migration `20260413000010_tier1_hardening.sql`), so the daily delete is index-only and never table-scans. Idempotent — re-running the same day is a no-op against the empty result set. The ledger remains the source of truth for in-flight idempotency (Phase 0.5 invariant); only fully terminal rows are pruned.
- **Daily reconciliation report (Tier 1 hardening #11 — 2026-04-13):** `daily-recon-summary` (cron `0 12 * * *` UTC) aggregates the prior 24h of operational health into a single structured log line and (if `OPS_ALERT_EMAIL` is set) a plaintext email digest via Resend: `external_sync_events` success/error counts grouped by system, `warehouse_review_queue` open items grouped by severity, and `channel_sync_log` failure/partial counts grouped by channel. Read-only by contract — never mutates state. Report rendering (`renderReportBody`) is a pure function so it is unit-tested independently of the email transport. The cron is the day-zero ops dashboard; alert thresholds living in `docs/operations/SLO.md` reference these counters as their data source.
- **Weekly backup verification probe (Tier 1 hardening #8 agent slice — 2026-04-13):** `weekly-backup-verify` (cron `0 9 * * 0` Sundays UTC) is a sensor-grade liveness check on the critical-table set (`workspaces`, `warehouse_inventory_levels`, `warehouse_product_variants`, `warehouse_orders`, `external_sync_events`). For each table it asserts non-zero `count(*)` and verifies the most-recent timestamp column (`created_at` / `updated_at` / `started_at`) is within a per-table freshness threshold (orders ≤ 14 days, sync events ≤ 7 days, etc.). Stale or empty tables emit alert-level log lines that the SLO doc maps to PagerDuty. The agent task does NOT exercise full sandbox restore — that is operator-led and documented in `docs/operations/runbooks/backup-verify.md`. Together, the agent probe (aliveness) and the operator runbook (restorability) constitute the full Tier 1 #8 contract.
- **Service-role usage is documented and CI-enforced (Tier 1 hardening #2 — 2026-04-13):** every `createServiceRoleClient()` call site in `src/**` is enumerated in `docs/security/service-role-usage.md` with a justification (Trigger task / webhook / OAuth handler / Server Action / shared lib). `scripts/check-service-role-usage.sh` (CI) greps the source tree for `createServiceRoleClient` (excluding tests + the helper itself), counts unique files, and fails the build if the count drifts from the manifest entry count. Adding a new service-role usage requires a manifest entry in the same PR — preventing accidental RLS bypass from sneaking into the codebase undocumented.
- **Tenant isolation is integration-tested (Tier 1 hardening #3 — 2026-04-13):** `tests/integration/tenant-isolation.test.ts` (run via `pnpm test:integration` against `vitest.integration.config.ts`) seeds two synthetic workspaces with overlapping data shapes via the service-role client, then attempts cross-workspace reads from the anon client authenticated as workspace-A's user. Asserts RLS denies access to workspace-B's `warehouse_products`, `warehouse_inventory_levels`, and adjacent org-scoped tables. The test skips gracefully if `SUPABASE_INTEGRATION_*` env vars are unset (so unit-test suites stay decoupled), and is wired into the release-gate when integration secrets are available.
- **Sentry tracing wraps the inventory fanout (Tier 1 hardening #4 — 2026-04-13):** `fanoutInventoryChange` opens a `Sentry.startSpan({ name: 'inventory.fanout', op: 'fanout' })` with `workspace_id`, `sku`, `delta`, and `correlation_id` as attributes, and each Shopify push is wrapped in a child span. Errors inside the span auto-capture to Sentry (Trigger.dev tasks already initialise `@sentry/node` per Rule #49). The span graph is the single observability handle for "what happened during a fanout" — no need to stitch together log lines across integrations.
- **Phase 5 tiered reconcile + canonical sync-state view (2026-04-13):** Steady-state safety net for the bidirectional bridge. Three schedule wrappers — `shipstation-bandcamp-reconcile-hot` (cron `*/5 * * * *`), `shipstation-bandcamp-reconcile-warm` (`*/30 * * * *`), `shipstation-bandcamp-reconcile-cold` (`0 */6 * * *`), all pinned to `shipstationQueue` (concurrencyLimit 1, shared with seed + SHIP_NOTIFY) — call the same inner runner `runShipstationBandcampReconcile(tier, payload, ctx, deps)` in `src/trigger/tasks/shipstation-bandcamp-reconcile.ts`. Tier candidate selection: hot = SKUs with `available <= HOT_LOW_STOCK_THRESHOLD (10)` UNION SKUs with `warehouse_inventory_activity` rows where `delta < 0` in the last 24h; warm = SKUs with `delta < 0` in the last 30 days; cold = full per-workspace catalog. Every tier excludes bundle parents (handled by Phase 2.5(c) `bundle-derived-drift-sensor`) and short-circuits with `skipped_no_v2_defaults` when `workspaces.shipstation_v2_inventory_warehouse_id` / `_location_id` are NULL. v2 reads use the batch-only `listInventory({ skus, inventory_warehouse_id, inventory_location_id })` (Rule §7.1.6 — NEVER per-SKU `getInventoryBySku`); a SKU at `available: 0` is invisible to v2 (Phase 0 §4.2.3) so a missing v2 row is treated as `available: 0`, NOT as drift. Drift thresholds (plan §7.1.6.1): `|drift| <= SILENT_DRIFT_TOLERANCE (1)` → silent auto-fix, no review item; `2 <= |drift| <= HIGH_SEVERITY_DRIFT_THRESHOLD (5)` → auto-fix + low-severity review queue row; `|drift| > 5` → auto-fix + high-severity review queue row (surfaced on the admin Channels page). Auto-fix is ALWAYS through `recordInventoryChange({ source:'reconcile', correlationId:'reconcile:{tier}:{run_id}:{sku}' })` — the Phase 5 schema migration (`20260413000030`) admits `'reconcile'` into `warehouse_inventory_activity_source_check`. v2 is the source of truth at this layer; reconcile NEVER writes to v2. Review queue dedup uses `group_key = 'reconcile.qty_drift:{workspace_id}:{sku}'` (Rule #55 — re-detection on the existing OPEN row bumps `occurrence_count` and may escalate severity low → high if drift widens). Per-tier `channel_sync_log` rows (`channel='shipstation_v2'`, `sync_type='reconcile_{tier}'`) drive the admin monitoring page (`/admin/settings/shipstation-inventory`). Companion view `sku_sync_status` (Phase 5 migration §7.1.13) denormalizes per-variant Bandcamp mapping state, baseline-anomaly status, sku-conflict status, last successful v2/Bandcamp push timestamps, and last external error into a single read so admin pages don't reconstruct truth from 6+ joins per request. Live view today; materialization deferred until any admin surface exceeds 500ms p95 (Open Question #9). The reconcile sensor and the Phase 4 focused legs are complementary: focused legs catch real-time changes; reconcile catches what focused legs drop.
- **ShipStation Inventory Sync echo contract (2026-04-13 second-pass audit):** The operator activated **ShipStation Inventory Sync** for every connected Shopify / Squarespace / WooCommerce store AND ShipStation has native Bandcamp store integrations registered in `warehouse_shipstation_stores` (10+ Bandcamp stores across 23 SS stores total at audit time). SS Inventory Sync subscribes directly to each storefront's order/inventory webhooks and decrements v2 natively at import time — completely independent of our app's webhook processing. Therefore the canonical echo-skip set in `src/lib/server/inventory-fanout.ts` (`SHIPSTATION_V2_ECHO_SOURCES`) MUST include EVERY storefront source that SS Inventory Sync mirrors: `shipstation` (SHIP_NOTIFY processor), `reconcile` (Phase 5 sensor), `shopify`, `squarespace`, `woocommerce`, `bandcamp`. Warehouse-side write sources (`manual`, `manual_inventory_count`, `cycle_count`, `inbound`, `preorder`, `backfill`) are NOT in the echo set — those originate in our app and v2 has not yet seen them, so they MUST fanout. The companion implication for sale polls: `bandcamp-sale-poll` does NOT enqueue `shipstation-v2-decrement` (the explicit Phase 4 trigger was removed in the second-pass audit — SS already decremented v2 when it imported the order). The `shipstation-v2-decrement` task code is preserved as a manual rollback path; if SS Inventory Sync is ever disabled per-workspace, BOTH the explicit enqueue in `bandcamp-sale-poll` AND the `'bandcamp'` membership in `SHIPSTATION_V2_ECHO_SOURCES` must be reverted together (never one without the other — the comments in both files cross-reference each other). Phase 5 reconcile remains the safety net for any drift the echo skip introduces.
- **Phase 4 bidirectional bridge (2026-04-13, amended 2026-04-13 second-pass audit):** Per-SKU cross-system inventory mirroring runs through two ledger-gated, queue-pinned tasks instead of inline calls. (1) **`shipstation-v2-decrement`** (`shipstationQueue`, concurrencyLimit 1): formerly triggered by `bandcamp-sale-poll` after each successful `recordInventoryChange()` for a Bandcamp sale; the explicit enqueue was removed in the second-pass audit (see "ShipStation Inventory Sync echo contract" above). Task code is preserved for manual rollback / future per-workspace activation. Correlation-id shape `sale:{band_id}:{package_id}:{newSold}:{sku}`. Skip cascade: `fanout-guard.shouldFanout("shipstation", correlationId)` → `workspaces.shipstation_v2_inventory_warehouse_id`/`_location_id` IS NOT NULL → variant resolved → variant is NOT a `bundle_components.bundle_variant_id` (Phase 2.5(a) bundle exclusion) → ledger acquired (`UNIQUE(system='shipstation_v2', correlation_id, sku, action='decrement')`). On the success path, calls `adjustInventoryV2({ transaction_type:'decrement', quantity:|delta| })` — NEVER `modify` (Phase 0 Patch D2 — `modify new_available:0` returns 400). On failure, marks the ledger row `error` and re-throws so Trigger.dev retries the task (the same correlation_id makes the retry a no-op once the prior attempt eventually succeeds elsewhere). (2) **`bandcamp-push-on-sku`** (`bandcampQueue`, concurrencyLimit 1, OAuth-serialized — Rule #9): triggered by `process-shipstation-shipment` after each per-line successful `recordInventoryChange()`. Correlation-id shape `ship:{shipmentId}:{sku}`. Skip cascade: `fanout-guard.shouldFanout("bandcamp", correlationId)` → variant resolved → NOT distro (`warehouse_products.org_id IS NOT NULL`) → variant is NOT a bundle parent → `bandcamp_product_mappings` row exists → `push_mode IN ('normal','manual_override')` (Phase 1 contract) → mapping is package-level (option-level mappings defer to the 5-min `bandcamp-inventory-push` cron) → ledger acquired (`UNIQUE(system='bandcamp', correlation_id, sku, action='modify')`). On the success path, computes `pushed_quantity = MAX(0, available - effective_safety)` from `warehouse_inventory_levels` (identical formula to the cron path so cron + focused push agree), refreshes the OAuth token, and calls `updateQuantities` for the single item. The cron `bandcamp-inventory-push` (every 5 min) remains the safety net for any focused enqueue that fails or short-circuits. Both new tasks consult `loadFanoutGuard` (Tier 1 #1/#13 — kill switches + percentage rollout via FNV-1a deterministic correlation-id bucketing) so Phase 4 ramp posture is `fanout_rollout_percent = 0` per workspace at deploy and operators ramp 0 → 10 → 50 → 100 per integration. Workspaces without `shipstation_v2_inventory_warehouse_id` / `_location_id` set short-circuit cleanly with status `skipped_no_v2_defaults` — Phase 3 admin populates these on first successful seed. Bundle SKUs are intentionally excluded from BOTH legs (Phase 2.5(a) for v2; bundle-aggregated focused push is deferred to the cron path because the cron has the bundle math hot loop). The `external_sync_events` table is the single source of truth for "did we already push this delta to that system" — retries, replays, and webhook redeliveries collide on the unique key and become no-ops.
- **Shipping log list/detail parity invariant:** `getShipments` and `getShipmentDetail` MUST use the same fulfillment cost model (postage + materials + pick/pack), computed via `src/lib/server/shipment-fulfillment-cost.ts`. The list enriches each row with `fulfillment_total` and `fulfillment_partial`; the detail returns `costBreakdown.total` and `costBreakdown.partial`. All variant and format-cost queries MUST scope by `workspace_id`. No direct SQL to `warehouse_product_variants` or `warehouse_format_costs` for cost computation outside this module. The admin Shipping Log cost column and profitability dot show the fulfillment total (not postage-only). The dot is amber when `partial=true`; green/red when customer shipping is known and costs are complete.
- **Per-connection cutover state machine — Pass 1 substrate (Phase 3 Pass 1 — 2026-04-23):** `client_store_connections` carries `cutover_state` (`text`, NOT NULL DEFAULT `'legacy'`, CHECK ∈ {`legacy`, `shadow`, `direct`}) plus audit columns (`cutover_started_at`, `cutover_completed_at`, `shadow_mode_log_id`, `shadow_window_tolerance_seconds` bounded 30–600s). The column is **orthogonal to `do_not_fanout`** (Phase 3 §9.4 truth table); the DB CHECK `client_store_connections_cutover_dormancy_check` rejects `(cutover_state IN ('shadow','direct'), do_not_fanout=true)`. The single chokepoint `shouldFanoutToConnection()` (`src/lib/server/client-store-fanout-gate.ts`) defensively rejects unrecognized `cutover_state` values with `invalid_cutover_state` and treats `null`/`undefined` as the DB default `'legacy'`. Companion tables landed in the same migration (`20260424000002_connection_cutover_state.sql`): `connection_shadow_log` (per-push ledger that Pass 2's comparison hook will fill — `pushed_quantity` / `pushed_at` / `ss_observed_quantity` / `observed_at` / `match` / `drift_units` / `cutover_state_at_push` / `metadata`) and `connection_echo_overrides` (operator-flippable `(connection_id, override_type, is_active)` rows with a partial unique index `uq_connection_echo_overrides_active`). Pass 1 echo-override read path is `resolveShipstationV2EchoSkip(source, originatingConnectionId, supabase)` in `src/lib/server/inventory-fanout.ts`: storefront sources (`shopify`, `squarespace`, `woocommerce`, `bandcamp`) consult the override table and flip echo-skip OFF when an active row exists; direct-v2 sources (`shipstation`, `reconcile`) ALWAYS echo-skip regardless of any override (Rule #65 — pushing those back is the literal echo loop); DB lookup failures fail CLOSED (still echo-skip) so a transient error cannot silently re-introduce double-decrement. `originatingConnectionId` is plumbed through `recordInventoryChange` → `fanoutInventoryChange`, with backward-compat fallback to `metadata.connection_id` for legacy callers. Server Actions `reactivateClientStoreConnection` and `disableStoreConnection` SELECT `cutover_state` and surface actionable operator errors before hitting the DB constraint. Section C.2 of `docs/RELEASE_GATE_CRITERIA.md` documents the schema + table presence checks; `scripts/check-release-gates.sh` enforces them. Pass 1 is **GA-safe and additive** — no fanout call site changes behavior unless an operator inserts an override row (Pass 2 will gate that behind the cutover wizard at `/admin/settings/connection-cutover`).
- **Per-connection cutover state machine — Pass 2 operator surface (Phase 3 Pass 2 — 2026-04-27):** Pass 2 closes the loop. The shadow-mode write hook `recordShadowPush()` (`src/lib/server/connection-shadow-log.ts`) fires from both branches of `client-store-push-on-sku` whenever `conn.cutover_state === 'shadow'`, inserting a `connection_shadow_log` row and enqueuing a delayed `shadow-mode-comparison` Trigger task (pinned to `shipstationQueue`, shares the v2 60 req/min budget). The comparison task re-reads ShipStation v2 inventory after `shadow_window_tolerance_seconds` (default 60s, bounded 30–600s by DB CHECK), persists `match` + `drift_units` back to the originating row, and writes a `metadata.skip_reason` for non-comparable cases (`no_v2_defaults`, `v2_read_failed`, etc.) so the diagnostics surface can bucket them. The Server Actions in `src/actions/connection-cutover.ts` form the wizard contract: `getCutoverDiagnostics(connectionId)` returns 7-day rolling counters + recent drift samples + comparison_skip_breakdown + gate evaluation (eligible iff match_rate ≥ 99.5% across ≥ 50 resolved comparisons, `MIN_SAMPLE_COUNT_FOR_CUTOVER` / `REQUIRED_MATCH_RATE` exported); `runConnectionCutover()` enforces three gates in order — (1) `cutover_state === 'shadow'`, (2) diagnostics eligible (bypassable with `force=true` + `forceReason ≥ 8 chars`), (3) no `external_sync_events` with `status='in_flight'` for this connection's sync system in the last 5 minutes — then inserts a `connection_echo_overrides` row carrying the diagnostics snapshot + operator id + force reason in `metadata` (column added by migration `20260427000003_connection_echo_overrides_metadata.sql`) BEFORE flipping `cutover_state='direct'`. Order matters: a crash between the two writes leaves the connection in `shadow` with an active override — safe (still in shadow logging mode) and auto-recoverable. `rollbackConnectionCutover()` deactivates active overrides and reverts to `legacy`. The wizard at `/admin/settings/connection-cutover` (sidebar entry under Settings) renders picker + diagnostics panel + three transition buttons; the cutover button is disabled unless the gate is eligible OR force-with-reason is supplied. Tests: `tests/unit/actions/connection-cutover.test.ts` (13 cases). Release-gate assertions C.2.3 / C.2.6 / C.2.7 are now active (drift retention index, override metadata column, shadow-window CHECK constraint).
- **Phase 5 §9.6 D1 — ATP layer substrate (2026-04-24):** `effective_sellable = MAX(0, available - committed - safety_stock[channel])` finally has a real `committed` term backing it. Migration `20260424000004_inventory_commitments.sql` added (a) `inventory_commitments` — append-mostly ledger keyed by `(workspace_id, source ∈ {order,cart,transfer,manual}, source_id, sku, qty>0, released_at)` with `metadata` JSONB; (b) `warehouse_inventory_levels.committed_quantity int NOT NULL DEFAULT 0 CHECK (>= 0)` — denormalized counter; (c) trigger `sync_committed_quantity()` (`AFTER INSERT/UPDATE/DELETE FOR EACH ROW`) keeps the counter in lockstep with open ledger rows inside the SAME transaction as each ledger mutation. Idempotency: UNIQUE partial index `uq_commitment_open_per_source_sku (workspace_id, source, source_id, sku) WHERE released_at IS NULL` — at most one OPEN commitment per logical source per SKU; a second `orders/create` retry hits the index and is a safe no-op. Trigger guards: `qty` cannot be mutated on an open row; rows cannot be un-released (audit invariant — flip is one-way). Hard-delete of an open row also decrements the counter so cleanup scripts can't leak committed quantity. The application-side helper is `src/lib/server/inventory-commitments.ts` (`commitInventory()` + `releaseInventory()`); it aggregates duplicate SKUs in the input into a single ledger row at the summed qty (so two line items for the same SKU don't trip the unique index and silently undercount), filters non-positive / empty-SKU items, and uses `upsert(..., { onConflict: ..., ignoreDuplicates: true })` so retries are safe. `releaseInventory()` only updates rows where `released_at IS NULL`, so a duplicate cancel webhook returns `released: 0`. The Phase 1 push helper `computeEffectiveSellable()` (`src/lib/server/effective-sellable.ts`) was updated to read `committed_quantity` alongside `available` and `safety_stock` — the snapshot is transactionally consistent because all three columns live on the same row. Crucially, `committed_quantity` is **exempt from Rule #20** ("single inventory write path") because the trigger is the ONLY writer; application code never touches the column. The legacy `warehouse_inventory_levels.committed integer` column (Phase 0 placeholder, audited 2026-04-24 as never written non-zero anywhere in the codebase) is left in place and will be dropped in a future migration once we are confident no read path treats it as meaningful. Live trigger lockstep verified end-to-end against production by `scripts/_phase5-d1-trigger-smoke.ts` (7/7 checks: insert→counter+, dup-blocked, qty-mutation-blocked, release→counter back to baseline, re-insert after release re-increments, un-release blocked, cleanup back to baseline). 16 unit tests in `tests/unit/lib/server/inventory-commitments.test.ts` pin the application helper contract. Substrate is GA-safe and additive; no production write path consumes it yet — the next sub-pass wires `commitInventory()` into the orders/create handlers (Shopify + client-store + Bandcamp + manual orders) and `releaseInventory()` into the orders/fulfill + orders/cancel + preorder-fulfillment paths.
- **Phase 5 §9.6 D1.b/c — ATP wire-up + counter recon (2026-04-24):** D1.b plumbed the substrate's helpers into the live order paths. `src/lib/server/inventory-commitments.ts` gained two convenience wrappers — `commitOrderItems({ workspaceId, orderId, items, metadata })` and `releaseOrderItems({ workspaceId, orderId, reason, skus? })` — both pin `source='order'` + `source_id=warehouse_orders.id` so the unique partial index `uq_commitment_open_per_source_sku` dedups retries automatically. `process-client-store-webhook.handleOrderCreated` calls `commitOrderItems()` immediately AFTER the `warehouse_order_items` insert (so a failed insert never opens a phantom commit) and `handleOrderCancelled` calls `releaseOrderItems({ reason: 'order_cancelled' })` after recrediting `available`. `mark-platform-fulfilled` (every platform branch) calls `releaseOrderItems({ reason: 'platform_fulfilled:{source}' })` AFTER the order's `platform_fulfillment_status` flip. All three call sites are wrapped in try/catch + `sensor_readings('inv.committed_*_failed', status='warning')` so a commit-ledger failure never fails the underlying order/cancel/fulfill — Rule #43 inventory event ordering is preserved. Bandcamp commit/release deferred (the sale-poll path semantically packs +sold deltas, not an order lifecycle, and the preorder/refund paths need their own design pass). The double-counting trap was solved by `workspaces.atp_committed_active boolean NOT NULL DEFAULT false` (migration `20260424000005_atp_committed_active_flag.sql`): `computeEffectiveSellable()` reads the flag and ONLY subtracts `committed_quantity` when it's `true`. Default `false` preserves the existing decrement-at-orders/create semantic exactly — the ledger is now populated for full audit/visibility but doesn't alter push values until Phase 5 §9.6 D1.b.1 refactors `available` to no longer decrement at order creation. Result snapshot now exposes `committedActive` + `committedSubtracted` for diagnostic flame graphs. **D1.c safety net:** `inventory-committed-counter-recon` (`src/trigger/tasks/inventory-committed-counter-recon.ts`, `schedules.task` cron `15 4 * * *` UTC, no queue pin — read-only across all workspaces) compares `warehouse_inventory_levels.committed_quantity` against `SUM(inventory_commitments.qty WHERE released_at IS NULL)` per `(workspace_id, sku)`. Drift is reported as `severity='high'` `warehouse_review_queue` rows (`category='inv_committed_counter_drift'`, `group_key='inv-committed-drift:{ws}:{sku}'` so the unique constraint `uq_review_queue_group_key` increments occurrence_count on repeats) AND a per-workspace `sensor_readings('inv.committed_counter_recon')` row (healthy/warning) for dashboard charting. The recon is INDEPENDENT of `atp_committed_active` — trigger correctness is independent of consumer-side math, so drift detection runs unconditionally. NEVER auto-corrects; surfaces drift for forensic review. Pure-data core extracted as `runInventoryCommittedCounterRecon({ supabase, reconRunId, pageSize })` with 8 unit tests (`tests/unit/trigger/inventory-committed-counter-recon.test.ts`) covering zero-drift, over-committed, under-committed, orphan ledger, multi-workspace mixed-status, NULL-treats-as-zero, pagination, and empty-catalog. All 66 Phase 5 D1 tests pass; typecheck clean; biome clean. Substrate + wire-up + recon are now end-to-end; the only consumer-side change behind `atp_committed_active=true` remains gated for the future semantic refactor.
- **Phase 5 §9.6 D2 — per-channel safety stock workspace (2026-04-24):** Operator surface for editing the third term in `effective_sellable = MAX(0, available - committed - safety_stock[channel])`. Storefront safety stock lives PER ROW on `client_store_sku_mappings.safety_stock` (smallint, CHECK >= 0) — no JSONB overlay, so the row is the single read/write target for both the §9.6 push helper AND the editor. Internal channels (`bandcamp`, `clandestine_shopify` — `INTERNAL_SAFETY_STOCK_CHANNELS` in `src/lib/shared/constants.ts`) write to `warehouse_safety_stock_per_channel(workspace_id, variant_id, channel)`; rows reverting to 0 on internal channels are DELETED to keep the table sparse for the push helper, but the audit log still captures `new_safety_stock=0` so the reversion is forensically visible. New migration `supabase/migrations/20260427000004_safety_stock_audit_log.sql` adds `warehouse_safety_stock_audit_log` — append-only history with `(channel_kind ∈ {storefront,internal} XOR connection_id|channel_name)` constraint, `prev_/new_safety_stock`, `prev_/new_preorder_whitelist`, `reason text`, `source ∈ {ui_inline,ui_bulk,ui_csv,system}`, `changed_by`, `changed_at`, partial indexes on `(workspace_id, changed_at DESC)` + `(workspace_id, sku, changed_at DESC)`, staff-only RLS. Audit rows are written by the Server Action (NOT a DB trigger) because the trigger cannot see the `reason` + `source` fields the UI captures. Single Server Action file `src/actions/safety-stock.ts` exports the full surface: `listSafetyStockChannels` (storefronts + internal channels with `policyDriftCount` = mappings where `last_inventory_policy='CONTINUE' AND preorder_whitelist=false`), `listSafetyStockEntries` (paginated, joins variant + inventory + last-edit timestamp from audit log), `updateSafetyStockBulk` (≤200 edits/call per Rule #41 — best-effort per-SKU so a renamed SKU on row 73 doesn't poison rows 1-72 + 74-200; outcomes returned per-SKU; storefront calls also accept `newPreorderWhitelist`; internal-channel calls IGNORE the field rather than reject), `previewSafetyStockCsv` / `commitSafetyStockCsv` (RFC 4180 parser at `src/lib/shared/safety-stock-csv.ts` — kept outside `"use server"` per the Next.js 14 rule), `listSafetyStockAuditLog` (filterable). UI page `src/app/admin/settings/safety-stock/page.tsx` (sidebar entry between Carrier Mapping and Feature Flags) renders: channel picker tabs (drift badge per storefront), search + dirty-edit count + Save/Discard toolbar, paginated grid with inline-editable safety_stock numeric input (Save batches dirty rows in one bulk call), and valid inline values stage immediately on change so the page-level Save button reflects dirty state before blur; read-only preorder + policy badges, per-row Sheet drawer for preorder_whitelist + reason + per-SKU audit history, modal CSV import with preview classification (create/update/delete/no_op/error) and reason field, workspace-wide audit Sheet with channel + SKU filters. Crucial Rule #20 invariant: this file is a POLICY editor — it NEVER calls `recordInventoryChange()`, never touches `available`/`committed_quantity`/Redis. Rule #58 (one truth per concern) — every safety_stock write funnels through this Server Action file. Companion test `tests/unit/actions/safety-stock.test.ts` (21 cases per Rule #6) covers parseCsv, channel listing, bulk update (staff-only, cross-workspace guard, no-op skip, SKU-not-found, audit row payload), CSV preview classification, commit delegation, and audit list filters.
- **Format resolution fallback chain:** When `warehouse_product_variants.format_name` is NULL, `batchBuildFormatCostMaps` falls back to `warehouse_products.product_type` via FK join (zero extra query). `product_type` is normalized through `PRODUCT_TYPE_ALIASES` (e.g. `"12\" Vinyl"` → `"LP"`) before lookup. If both are NULL or the normalized value has no cost row, the SKU is added to `unknownSkus` and `partial: true` is returned (amber dot). Migration `20260415000001_backfill_variant_format_names.sql` backfilled existing null rows: Tier 1 = SKU prefix patterns; Tier 2 = product_type alias map. Post-migration result: 418 genuinely ambiguous variants remain NULL (correctly → amber). `T-Shirt` and `Other` format rows are missing from `warehouse_format_costs` and need to be added in admin for those 4 variants to show green/red.

## Preflight Commands

Run these before substantive planning/building:

```bash
pnpm check
pnpm typecheck
pnpm release:gate
pnpm test:e2e:full-audit
```

For production parity/ops checks, also use:

- `scripts/sql/prod_parity_checks.sql`
- `scripts/sql/webhook_health_snapshot.sql`

## Doc Sync Contract (Mandatory)

Any session that changes behavior must update truth docs in the same session.

- Architecture or ownership changed -> update `TRUTH_LAYER.md` and `project_state/engineering_map.yaml`
- User/system flow changed -> update `project_state/journeys.yaml`
- API route/action boundary changed -> update `docs/system_map/API_CATALOG.md`
- Trigger task/event/cron wiring changed -> update `docs/system_map/TRIGGER_TASK_CATALOG.md`
- Cache/freshness/key policy changed -> update `docs/system_map/CACHE_ARCHITECTURE.md`
- Release or verification rules changed -> update `docs/RELEASE_GATE_CRITERIA.md`

Session completion must include:

1. Code changes summary
2. Truth docs updated list
3. Journey updates list
4. API/Trigger catalog updates list

## Ownership

- Architecture truth: engineering lead + primary maintainer
- API catalog: backend/app-router owners
- Trigger catalog: integrations/ops owners
- Journeys: product + engineering
