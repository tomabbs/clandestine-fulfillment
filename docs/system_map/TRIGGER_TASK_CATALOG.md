# Trigger Task Catalog

Canonical Trigger.dev task map for planning/build/audit.

## Runtime Baseline

- Config: `trigger.config.ts`
- Task root: `src/trigger/tasks`
- Registry: `src/trigger/tasks/index.ts`

### Operational cutover verification (2026-04-28)

Pre-go-live staging smoke spans **ShipStation order/shipment ingestion** (`shipstation-*` tasks — see table rows below), **`post-label-purchase`** orchestration (`src/trigger/tasks/post-label-purchase.ts`), **`easypost-register-tracker`**, and downstream **`send-tracking-email`**. Canonical operator checklist lives in **`docs/RELEASE_GATE_CRITERIA.md` § Operational cutover — staging smoke (ShipStation + EasyPost)** alongside TRUTH_LAYER § Operational cutover semantics.

## Queues and Concurrency

| Queue | File | Concurrency |
|---|---|---|
| `bandcamp-api` | `src/trigger/lib/bandcamp-queue.ts` | `1` |
| `bandcamp-sweep` | `src/trigger/lib/bandcamp-sweep-queue.ts` | `1` |
| `bandcamp-scrape` | `src/trigger/lib/bandcamp-scrape-queue.ts` | `5` |
| `shipstation` | `src/trigger/lib/shipstation-queue.ts` | `1` |

`bandcamp-scrape-page` (`bandcamp-sync.ts`): HTML fetch capped at **15s** in client; task `maxDuration` **60s** (parse, DB, optional Shopify description push).

> ShipStation integration restored in bridge period (2026-03-30) for transition until Shopify app approval. Shipment poll runs every 30 min, deduplicates via `shipstation_shipment_id`.

## Scheduled Tasks (Cron)

| Task ID | File | Schedule |
|---|---|---|
| `support-escalation` | `src/trigger/tasks/support-escalation.ts` | `*/5 * * * *` — **Support Inbox 2.0 (2026-04-25):** SLA-policy-aware reminder loop. Skips `sla_paused=true` and snoozed tickets, alerts staff before `next_response_due_at` / `resolution_due_at`, marks `sla_breached_at` after breach, writes `support_conversation_events`, and keeps the existing bounded client reminder path for `waiting_on_client`. SLA pause/resume duration is DB-trigger owned (`support_sla_pause_on_status_change`), not recalculated in the task. |
| `support-delivery-recovery` | `src/trigger/tasks/support-message-delivery.ts` | `*/5 * * * *` — retries failed `support_message_deliveries` rows where `attempt_count < 5` and `next_retry_at <= now()`, then re-fires `support-message-delivery` by `messageId`. |
| `shopify-sync` | `src/trigger/tasks/shopify-sync.ts` | `*/15 * * * *` |
| `shopify-order-sync` | `src/trigger/tasks/shopify-order-sync.ts` | `*/30 * * * *` |
| `bandcamp-sale-poll` | `src/trigger/tasks/bandcamp-sale-poll.ts` | `*/5 * * * *` — **2026-04-13 second-pass audit:** explicit `tasks.trigger("shipstation-v2-decrement", …)` was REMOVED from the post-sale fanout. ShipStation Inventory Sync (active for the native Bandcamp store integration registered in `warehouse_shipstation_stores`) imports the Bandcamp order and decrements v2 natively before this poll fires; the explicit enqueue would double-decrement (Rule #65 echo loop). The companion echo skip lives in `SHIPSTATION_V2_ECHO_SOURCES` inside `src/lib/server/inventory-fanout.ts` for `source='bandcamp'` — both layers MUST be reverted together if SS Inventory Sync is ever disabled per-workspace. Static-source regression guard: `tests/unit/trigger/bandcamp-sale-poll-no-v2-enqueue.test.ts` (extended Phase 2 to also scan `bandcamp-sale-poll-per-connection.ts` and the shared runner). The post-sale fanout still enqueues `bandcamp-inventory-push` and `multi-store-inventory-push`, plus `triggerBundleFanout` for bundle parents. **Phase 2 (2026-04-21):** body refactored to delegate to the shared `pollOneBandcampConnection()` runner (`src/trigger/lib/bandcamp-sale-poll-runner.ts`) so this cron stays in lock-step with the new event-driven `bandcamp-sale-poll-per-connection` task on idempotency contract, post-sale fanout, and v2-skip rationale. Cron remains the safety net for connections without populated `inbound_forwarding_address` and for any inbound email Resend drops. |
| `bandcamp-sale-poll-per-connection` | `src/trigger/tasks/bandcamp-sale-poll-per-connection.ts` | `src/lib/server/resend-inbound-router.ts → dispatchBandcampOrderPoll()` (event-driven, fired by Resend inbound webhook on `Bam!` / `Cha-ching!` order emails when the recipient address matches exactly one `bandcamp_connections.inbound_forwarding_address`) — **Phase 2 (2026-04-21):** event-driven sibling of `bandcamp-sale-poll`. Pinned to the shared `bandcampQueue` (Rule #9 — `concurrencyLimit:1` enforced across the cron + per-connection paths). Validates `{ workspaceId, connectionId, triggeredByWebhookEventId?, recipient? }` with Zod, looks up the row (rejects mismatched `workspace_id` as defense-in-depth against hand-fired runs), refreshes the OAuth token via `refreshBandcampToken(workspaceId)`, and delegates to `pollOneBandcampConnection()` so it cannot drift from the cron's idempotency / fanout / v2-skip contract. Writes one `channel_sync_log` row per run tagged `{ trigger: 'inbound_email', triggered_by_webhook_event_id, recipient }` for forensics. Companion test: `tests/unit/trigger/bandcamp-sale-poll-per-connection.test.ts`. |
| `bandcamp-inventory-push` | `src/trigger/tasks/bandcamp-inventory-push.ts` | `*/5 * * * *` — **Phase 1 (2026-04-17):** filters mappings to `push_mode IN ('normal','manual_override')` at the source. `blocked_baseline` and `blocked_multi_origin` rows are dropped before any `update_quantities` call (logged per-connection for operational visibility). See TRUTH_LAYER "Bandcamp push_mode contract". |
| `bandcamp-baseline-audit` | `src/trigger/tasks/bandcamp-baseline-audit.ts` | `0 3 * * *` — **Phase 1 (2026-04-17):** daily Bandcamp baseline anomaly + multi-origin push_mode audit. Pinned to `bandcamp-api` queue (Rule #9). Pass 1: per-mapping baseline detection from `raw_api_data.quantity_available` vs `sum(bandcamp_origin_quantities[].option_quantities[].quantity_available)` — no API call. Anomalies upsert into `bandcamp_baseline_anomalies` (keyed by `workspace_id, band_id, package_id, option_id` via two partial unique indexes for the NULL-vs-non-NULL option case); auto-flips `bandcamp_product_mappings.push_mode` between `normal` and `blocked_baseline`. Pass 2: per-band multi-origin probe via `getShippingOriginDetails` — if `> 1` origin, flips `normal → blocked_multi_origin`; if `≤ 1`, clears `blocked_multi_origin → normal`. `manual_override` is **NEVER** auto-cleared. Suggest-don't-mutate: never calls `update_quantities` or `update_sku`. Invokable on-demand via `forceBaselineScan` Server Action. |
| `bandcamp-scrape-sweep` | `src/trigger/tasks/bandcamp-scrape-sweep.ts` | `*/10 * * * *` |
| `bandcamp-sync-cron` | `src/trigger/tasks/bandcamp-sync.ts` | `*/30 * * * *` — Product creation now includes: price, cost (50% of Bandcamp price), inventory tracking (tracked=true), weight (category default), vendor collection assignment, and publishing to Online Store + Shop channels. **Dupe hardening (2026-04-19):** unmatched path now runs DB-first (`warehouse_products` with NULL Shopify ID → `warehouse_product_variants` with explicit error handling → `bandcamp_product_mappings`) and only then calls Shopify `productSetCreate`, then patches `warehouse_products.shopify_product_id`. Runtime guards: task exits early when `workspaces.bandcamp_sync_paused=true`; unmatched create re-validates package mapping ownership before Shopify create; if Shopify create succeeds but post-create DB attach verification fails, product is auto-archived and a high-severity review queue item is upserted. **Apparel multi-variant create (Phase A, 2026-04-20):** packages whose `options[]` exposes ≥2 distinct option SKUs (apparel sizes, color variants, etc.) now flow through a dedicated branch that materializes each option as its own `warehouse_product_variants` row + Shopify variant under a single inferred Shopify option (`Size` / `Color` / `Variant` — never `Title`). Per-option attribution is recorded on `warehouse_product_variants.bandcamp_option_id` + `bandcamp_option_title` (migration `20260420000005_variant_bandcamp_option.sql`). The single mapping row points at the first option as "primary" because `uq_bandcamp_mappings_workspace_item_id` allows only one mapping per package. Inventory seeded per option through `recordInventoryChange()`; `shopify_variant_id` + `shopify_inventory_item_id` back-filled by SKU after Shopify create. SKU collisions raise `bandcamp_apparel_sku_collision` review items instead of auto-merging. Pre-existing umbrella apparel (`tracked_as_metadata` audit class) is untouched. |
| `bandcamp-order-sync-cron` | `src/trigger/tasks/bandcamp-order-sync.ts` | `*/15 * * * *` — **B-1 (HRD-29) tighten (2026-04-22):** raised cadence from 6h to 15m for direct-Shopify cutover (BC orders are now the sole timing source — was ShipStation pre-cutover). Schedule trigger now passes a GLOBAL-scope `idempotencyKey` with `idempotencyKeyTTL: '15m'` so overlapping runs collapse instead of stacking on `bandcampQueue`. Module-scope `_lastTriggeredRunId` memo + `decideScheduleAction()` detect dedup and emit `sensor_readings` (`bandcamp.cron_idem_skip`, status `warning`) per workspace + structured `logger.warn` for flapping visibility. Cadence budget: ~120 req/run × 96 runs/day × N workspaces; revisit at N≥15 active bands. |
| `bandcamp-mark-shipped-cron` | `src/trigger/tasks/bandcamp-mark-shipped.ts` | `*/15 * * * *` |
| `client-store-order-detect` | `src/trigger/tasks/client-store-order-detect.ts` | `*/10 * * * *` — **Phase 0.8 (2026-04-17):** consults `shouldFanoutToConnection()` per row before polling; dormant connections (do_not_fanout / disabled_auth_failure / error / pending) short-circuit and emit a `dormant_skipped` log line. **Phase 8 (2026-04-27, SKU-AUTO-3):** after inserting `warehouse_orders` + `warehouse_order_items` for a newly discovered remote order, calls `runHoldIngressSafely` with `source: "poll"` (same helper as webhook order-create). Persists ingress warnings to `sensor_readings`; on `hold_applied` with `clientAlertRequired`, enqueues `send-non-warehouse-order-hold-alert`. Task return value includes `holdsApplied` + `alertsEnqueued` counts. Poll path does not decrement warehouse inventory (webhook remains canonical for stock movement). |
| `client-store-order-detect` Woo repair | `src/trigger/tasks/client-store-order-detect.ts`, `src/lib/clients/store-sync-client.ts` | **2026-04-28:** Woo polling now uses `createStoreReadClient()` so inbound reads are not blocked by outbound `do_not_fanout`. Cursor selection prefers `last_poll_processed_at`, then `last_poll_succeeded_at`, then `last_poll_at`, subtracts a 5-minute overlap, fetches Woo orders by `modified_after` / `orderby=modified` / `order=asc`, advances `last_poll_processed_at` per successfully processed order, and records `last_poll_attempted_at`, `last_poll_succeeded_at`, and `consecutive_poll_failures`. Webhook and poll ingestion share `ingestion_idempotency_key={platform}:{connection_id}:{external_order_id}` and reconcile only when the remote `external_order_modified_at` is newer. |
| `multi-store-inventory-push` | `src/trigger/tasks/multi-store-inventory-push.ts` | `*/5 * * * *` — **Phase 0.8 (2026-04-17):** filters connections through `shouldFanoutToConnection()` instead of just `connection_status = 'active'`. ShipStation Inventory Sync is now the canonical fanout path; first-party connectors are dormant (`do_not_fanout = true`) by default and only push when staff reactivate via `/admin/settings/client-store-reconnect`. |
| `sensor-check` | `src/trigger/tasks/sensor-check.ts` | `*/5 * * * *` — **updated 2026-04-10**: global stuck-import check auto-fails `warehouse_pirate_ship_imports` stuck in `pending`/`processing` for >30 min and creates a `severity: high` review queue item |
| `preorder-fulfillment` | `src/trigger/tasks/preorder-fulfillment.ts` | `0 6,18 * * *` (America/New_York) — 2×/day; tags-only model (no selling plans, §21) |
| `monthly-billing` | `src/trigger/tasks/monthly-billing.ts` | `0 2 1 * *` (America/New_York) |
| `storage-calc` | `src/trigger/tasks/storage-calc.ts` | `0 1 1 * *` (America/New_York) |
| `redis-backfill` | `src/trigger/tasks/redis-backfill.ts` | `0 3 * * 2` (America/New_York) |
| `external-sync-events-retention` | `src/trigger/tasks/external-sync-events-retention.ts` | `30 3 * * *` (America/New_York) — **Tier 1 hardening #14 (2026-04-13):** daily prune. Deletes `external_sync_events` rows with `status='success'` and `completed_at < now() - interval '7 days'`, plus `status='error'` rows older than 30 days. Backed by partial index `idx_external_sync_events_completed_status (status, completed_at) WHERE completed_at IS NOT NULL` (migration `20260413000010_tier1_hardening.sql`) so the cleanup never table-scans. Logs deletion counts; missing rows is a no-op (idempotent). Runbook: `docs/operations/runbooks/external-sync-events-retention.md`. |
| `daily-recon-summary` | `src/trigger/tasks/daily-recon-summary.ts` | `0 12 * * *` (UTC) — **Tier 1 hardening #11 (2026-04-13):** daily reconciliation report. Aggregates last-24h `external_sync_events` (success/error per system), open `warehouse_review_queue` items by severity, and `channel_sync_log` failures/partials per channel. Logs a structured report; if `OPS_ALERT_EMAIL` is set, also delivers a plaintext digest via Resend. Read-only; never mutates state. Runbook: `docs/operations/runbooks/daily-recon-summary.md`. |
| `weekly-backup-verify` | `src/trigger/tasks/weekly-backup-verify.ts` | `0 9 * * 0` (Sundays UTC) — **Tier 1 hardening #8 agent slice (2026-04-13):** weekly probe of critical tables (`workspaces`, `warehouse_inventory_levels`, `warehouse_product_variants`, `warehouse_orders`, `external_sync_events`). Asserts non-zero row counts and timestamp recency against per-table thresholds (e.g. orders within 14 days, sync events within 7 days). Logs alerts on stale/empty tables. Does NOT exercise the full sandbox restore — that remains an operator runbook (`docs/operations/runbooks/backup-verify.md`). Sensor-grade liveness check only. Runbook: `docs/operations/runbooks/weekly-backup-verify.md`. |
| `daily-scan-form` | `src/trigger/tasks/generate-daily-scan-form.ts` | `0 17 * * 1-6` (America/New_York) |
| `oauth-state-cleanup` | `src/trigger/tasks/oauth-state-cleanup.ts` | `*/15 * * * *` |
| `discogs-listing-replenish` | `src/trigger/tasks/discogs-listing-replenish.ts` | `0 * * * *` (hourly) |
| `discogs-mailorder-sync` | `src/trigger/tasks/discogs-mailorder-sync.ts` | `*/10 * * * *` |
| `discogs-client-order-sync` | `src/trigger/tasks/discogs-client-order-sync.ts` | `*/10 * * * *` |
| `discogs-message-poll` | `src/trigger/tasks/discogs-message-poll.ts` | `*/5 * * * *` |
| `shipstation-poll` | `src/trigger/tasks/shipstation-poll.ts` | `*/30 * * * *` — **hardened 2026-04-02**: upsert ON CONFLICT `(workspace_id, shipstation_shipment_id)`, pre-fetches `/orders` for `shippingAmount` → `customer_shipping_charged`, two-phase order auto-link (exact normalized order number → auto-assign; probabilistic → review queue only), ghost item pruning, `label_source='shipstation'`. **Updated 2026-04-13**: auto-copies `bandcamp_payment_id` from linked order, uses shared `normalizeOrderNumber`, writes `sensor_readings` on completion. **Updated 2026-04-15**: Step A upsert captures `error` explicitly; all errors logged with `code` + `message`; `23514` (CHECK constraint violation) creates `severity:high` review queue item with `group_key: ss_constraint_fail:{id}` — no silent shipment drops |
| `bundle-availability-sweep` | `src/trigger/tasks/bundle-availability-sweep.ts` | `0 6 * * *` (daily 6am UTC) |
| `catalog-stats-refresh` | `src/trigger/tasks/catalog-stats-refresh.ts` | `0 4 * * *` (daily 4am UTC) |
| `bandcamp-sales-sync` | `src/trigger/tasks/bandcamp-sales-sync.ts` | `0 5 * * *` (daily 5am UTC) |
| `bandcamp-sales-backfill-cron` | `src/trigger/tasks/bandcamp-sales-backfill.ts` | `*/10 * * * *` — self-healing monitor: detects stale running connections (>2h), retries up to 3 failed chunks per run using sync sales_report API, reads bandcamp_sales_backfill_log for gap detection. **Phase 0.0 hotfix (2026-04-13):** wired onto `bandcampQueue` (concurrencyLimit: 1) per Rule #9 to prevent concurrent `refreshBandcampToken()` calls from triggering `duplicate_grant` and destroying the OAuth token family. The on-demand `bandcamp-sales-backfill` task is also pinned to `bandcampQueue` for defense-in-depth. |
| `sku-sync-audit` | `src/trigger/tasks/sku-sync-audit.ts` | `0 2 * * *` — **Phase 0.5 (2026-04-17):** daily SKU mismatch detector. Pinned to `shipstation` queue. Runs three detectors per workspace: (1) casing conflicts on `warehouse_product_variants.sku`, (2) client-store mismatches against `client_store_sku_mappings` (flags Squarespace placeholders, generic mismatches), (3) ShipStation v1 product orphans via paginated `/products` (rate-limit aware). Conflicts upserted into `sku_sync_conflicts` keyed by `group_key`; re-detections bump `occurrence_count` and reopen resolved rows. Suggest-only — never mutates external systems. **Phase 2 SKU-matching workspace (2026-04-25):** `/admin/settings/sku-matching` consumes this table as an existing-conflicts overlay only; the new review workspace does not enqueue any Trigger task and intentionally keeps alias approval synchronous + RPC-backed. **Northern Spy Label Group coverage (2026-04-28):** multi-org `client_store_connection_org_coverage` expands only the staff SKU Matching read/accept scope; existing Trigger consumers continue to read live aliases from `client_store_sku_mappings` by connection/SKU/inventory item. `process-client-store-webhook`, `client-store-order-detect`, `client-store-push-on-sku`, `multi-store-inventory-push`, and `bandcamp-sync` require no code change for the coverage seed; umbrella order ownership remains `client_store_connections.org_id` until a separate order-attribution design exists. |
| `sku-matching-monitor` | `src/trigger/tasks/sku-matching-monitor.ts` | `0 14 * * 1` — **Phase 6 (2026-04-25):** weekly 7-day SKU-matching telemetry review. Reads `sku_matching_perf_events`, computes workspace-load p90, acceptance-vs-review-only rate, and conflict-count growth, then inserts a `sensor_readings` row (`sensor_name='sku_matching.monitoring'`) per workspace. If conflict growth crosses the rollout threshold (`latest_conflict_count >= 20` AND `growth >= 10` over the 7-day window), upserts a `warehouse_review_queue` row keyed `sku-matching-conflicts:{workspace_id}` so operators investigate before widening adoption. |
| `stock-stability-sampler` / `stock-stability-readings-purge` | `src/trigger/tasks/stock-stability-sampler.ts` | Sampler: `*/15 * * * *`. Purge: `0 4 * * *` — **Phase 5.A autonomous SKU matcher (2026-04-26):** sampler populates the `stock_stability_readings` time-series that Phase 5.B shadow-promotion and the Phase 4 webhook-rehydrate path read to enforce the stock-stability gate. Per workspace respects `workspaces.sku_autonomous_emergency_paused` (fail-closed on read errors); NOT pinned to `bandcamp-api`. Variant universe = UNION of `client_store_product_identity_matches` (non-terminal outcomes) + `client_store_sku_mappings` (all pushed variants) → batch-reads `warehouse_inventory_levels.available - committed_quantity` → `INSERT ... ON CONFLICT DO NOTHING` bucketed to the 15-minute mark so duplicate writes from overlapping runs are idempotent. Purge removes rows older than `STABILITY_WINDOWS_MS.boost` × safety factor daily. Pure shaping helpers in `src/lib/server/stock-stability-sampler.ts`. |
| `sku-shadow-promotion` / `sku-shadow-promotion-manual` | `src/trigger/tasks/sku-shadow-promotion.ts` | `0 3 * * *` (daily) — **Phase 5.B autonomous SKU matcher (2026-04-26):** daily shadow-to-live promotion sweeper. Queries `client_store_product_identity_matches` via the `idx_identity_matches_promotion_candidates` partial index (outcome_state `auto_database_identity_match`, `is_active=true`, `warehouse_stock_at_match > 0`), derives `PathAEvidenceFlags` defensively from `evidence_snapshot`, counts prior `sku_autonomous_decisions` for Path B, reads current warehouse ATP from `warehouse_inventory_levels`, reads 24h history from `stock_stability_readings` (filtered by `SAMPLER_WAREHOUSE_SOURCE`), and routes each candidate through the pure `shouldPromoteShadow()` policy (`src/lib/server/sku-shadow-promotion-policy.ts`). `action:'promote'` calls `promoteIdentityMatchToAlias()` (the single legal TS entry point — SKU-AUTO-8 — which handles its own decision + transition logging, the stability gate, and the advisory-locked RPC). `action:'bump'` increments `evaluation_count`/`state_version`/`last_evaluated_at` with OCC and writes a `sku_autonomous_decisions` row (`outcome_changed=false`) carrying the disqualifiers so the audit trail records why promotion was declined. One `sku_autonomous_runs` row per workspace per pass (`trigger_source='scheduled_periodic'`, `dry_run=false`, `feature_flags.entry_point='sku-shadow-promotion'`). Respects `workspaces.sku_autonomous_emergency_paused`. NOT pinned to `bandcamp-api` — pure DB + RPC, no outbound APIs. |
| `sku-autonomous-dry-run` | `src/trigger/tasks/sku-autonomous-dry-run.ts` | Manual only — **SKU-AUTO-18 dry-run runner (2026-04-27):** evaluates active Shopify / WooCommerce / Squarespace client-store connections without mutating production matching state. The task validates optional `{ workspaceId, connectionId, limitPerConnection, triggerSource }`, delegates to `src/lib/server/sku-autonomous-dry-run.ts`, honors `workspaces.sku_autonomous_emergency_paused`, fetches the same remote catalogs used by `/admin/settings/sku-matching`, ranks canonical variants with `rankSkuCandidates()`, opens one `sku_autonomous_runs` row per connection with `dry_run=true`, and writes one `sku_autonomous_decisions` row per evaluated variant. Safety boundary: it does NOT import or call alias promotion, outcome-transition RPCs, identity-match writes, order-hold RPCs, alert tasks, inventory writes, or live alias writes. The only writes are run/decision audit rows and final run summaries, so operators can inspect confidence/outcome breakdowns in the autonomous-runs surface before any autonomy flag is enabled. |
| `sku-hold-recovery-recheck` / `sku-hold-recovery-recheck-manual` | `src/trigger/tasks/sku-hold-recovery-recheck.ts` | `*/30 * * * *` — **Phase 5.C autonomous SKU matcher (2026-04-26):** 30-minute sweep that finds `warehouse_orders` rows held with `fulfillment_hold='on_hold'` AND `fulfillment_hold_reason='fetch_incomplete_at_match'` AND `fulfillment_hold_at >= now() - 24h` (order by `fulfillment_hold_at` ascending, capped at 200 rows per workspace per pass). For each order: resolves the `client_store_connections` row → calls `fetchRemoteCatalogWithTimeout()` as a platform health probe (fetch failure → hold retained, counted as `still_held`) → runs `loadNormalizedOrder(orderId, { source:'recovery' })` → runs `evaluateOrderForHold()`. When BOTH the fetch succeeds AND the evaluator returns `shouldHold=false`, releases via `releaseOrderFulfillmentHold({ resolutionCode:'fetch_recovered_evaluator_passed', actorKind:'recovery_task' })` (SKU-AUTO-17 whitelist, enforced by the RPC's CHECK on `p_resolution_code`). Respects `workspaces.sku_autonomous_emergency_paused` (the per-workspace pause — Rule SKU-AUTO-20 — blocks this task because it DOES write release events). NOT pinned to `bandcamp-api` — makes outbound Shopify/Woo/Squarespace API calls via `fetchRemoteCatalogWithTimeout()`, which manages its own per-platform timeouts on the default Trigger queue. Idempotent: the release RPC short-circuits already-released orders; a double delivery re-processes the same order and both workers observe idempotent successes. |
| `sku-autonomous-telemetry` / `sku-autonomous-telemetry-manual` | `src/trigger/tasks/sku-autonomous-telemetry.ts` | `0 8 * * 1` (weekly, Mon 08:00 UTC) — **Phase 7.B autonomous SKU matcher (2026-04-26):** weekly-per-workspace rollout telemetry sensor. Reads a 30-day window of `sku_autonomous_runs`, `sku_autonomous_decisions` (paginated), `sku_outcome_transitions`, `order_fulfillment_hold_events`, and the current `client_store_product_identity_matches` outcome breakdown; feeds them into the pure `summarizeAutonomousTelemetry()` helper (`src/lib/server/sku-autonomous-telemetry.ts`) which evaluates Phase 7 "Critical success criteria" thresholds (max_demotion_rate 2%, hold_released_rate band 60-80%, max_client_alerts_per_week 20, promotion_rate_monthly band 10-30%, min_decision_audit_completeness 100%, max_run_failure_rate 10%). Emits ONE `sensor_readings` row per workspace (`sensor_name='sku_autonomous.telemetry'`, status `healthy`/`warning`/`paused`) carrying the full rollup + identity-count breakdown + per-table truncation flags. For each threshold trip upserts a `warehouse_review_queue` row keyed `sku-autonomous-telemetry:{workspace_id}:{isoWeekKey}:{reason_code}` so a reason tripping multiple weeks yields one row per week. Respects `workspaces.sku_autonomous_emergency_paused` — sensor ALWAYS writes (observability must not drop during an incident) but review queue upserts are suppressed while paused. NOT pinned to `bandcamp-api` — pure DB reads + writes. Consumed by `/admin/settings/sku-matching/rollout` (Slice 7.D) via `getAutonomousRolloutHealth()`. |
| `sku-holdout-stop-condition-sweep` / `sku-holdout-stop-condition-sweep-manual` | `src/trigger/tasks/sku-holdout-stop-condition-sweep.ts` | `30 4 * * *` (daily) — **Phase 5.D autonomous SKU matcher (2026-04-26):** daily sweep that retires `client_store_product_identity_matches` rows stuck in `auto_holdout_for_evidence` meeting EITHER stop condition — `evaluation_count >= 10` (reason `holdout_expired_10_evaluations`) OR `age_days >= 90` (reason `holdout_expired_90_days`). When BOTH are true age wins because age is the harder rotation guarantee. One DB round-trip via `.or()` + defensive per-row `pickReason()` check (skips rows the `.or()` selected but that no longer meet a cap → counted as `skipped_not_stuck`). Transition via `applyOutcomeTransition({ from:'auto_holdout_for_evidence', to:'auto_reject_non_match', trigger:'periodic_revaluation', reasonCode })` — the RPC applies pg_advisory_xact_lock + `state_version` OCC for defense-in-depth against concurrent retirements. One `sku_autonomous_runs` row per workspace per pass; every candidate (success or failure) writes a `sku_autonomous_decisions` audit row with `previous_outcome_state='auto_holdout_for_evidence'`, `reason_code`, and on failure the disqualifier array carries the RPC error reason. Respects `workspaces.sku_autonomous_emergency_paused` — this task writes identity rows so the per-workspace pause MUST block it. NOT pinned to `bandcamp-api` — pure Postgres, no outbound APIs. |
| `scraper-reconcile` | `src/trigger/tasks/scraper-reconcile.ts` | `0 */6 * * *` — bulk review queue reconciliation: stale-success cleanup, category-mismatch resolution, dead URL lifecycle enforcement, monthly dead URL probes (bypass circuit breaker, suppress review items), category backfill, metrics emission to `sensor_readings` |
| `mailorder-shopify-sync` | `src/trigger/tasks/mailorder-shopify-sync.ts` | `*/30 * * * *` — syncs paid Shopify orders into `mailorder_orders` for consignment billing. Splits multi-org orders by SKU → variant → product.org_id, creating one row per (order × org). Payout = subtotal × 0.5 (excludes shipping). Uses 2-min overlap window for cursor safety. |
| `shipstation-bandcamp-reconcile-hot-schedule` | `src/trigger/tasks/shipstation-bandcamp-reconcile.ts` | `*/5 * * * *` — **Phase 5 (2026-04-13):** hot tier. Candidates = `warehouse_inventory_levels.available <= 10` UNION SKUs with negative `warehouse_inventory_activity` in last 24h. See on-demand row below for full skip cascade + drift threshold contract. |
| `shipstation-bandcamp-reconcile-warm-schedule` | `src/trigger/tasks/shipstation-bandcamp-reconcile.ts` | `*/30 * * * *` — **Phase 5 (2026-04-13):** warm tier. Candidates = SKUs with negative `warehouse_inventory_activity` in last 30 days. |
| `shipstation-bandcamp-reconcile-cold-schedule` | `src/trigger/tasks/shipstation-bandcamp-reconcile.ts` | `0 */6 * * *` — **Phase 5 (2026-04-13):** cold tier. Candidates = full per-workspace catalog (bundle parents excluded). Catches anything the hot/warm tiers missed. |
| `shipstation-v2-adjust-on-sku` | `src/trigger/tasks/shipstation-v2-adjust-on-sku.ts` | Triggered on demand from `submitManualInventoryCounts` Server Action AND from `fanoutInventoryChange()` for every non-echo, non-zero `recordInventoryChange()` write (audit fix F1, 2026-04-13) — **Saturday Workstream 2 (2026-04-18) + audit hardening (2026-04-13):** manual-count → ShipStation v2 sync bridge AND general v2 SKU-total fanout target. Sibling of `shipstation-v2-decrement` (which is sale-only, decrement-only); this task handles BOTH directions of delta because manual writes can go up or down. Pinned to `shipstation` queue (concurrency 1, shares the v2 60 req/min budget with seed/decrement/reconcile/spot-check). Skip cascade: (0) workspace `inventory_sync_paused=true` → `skipped_inventory_sync_paused` (audit fix F3, 2026-04-13); (1) `delta === 0` → `skipped_zero_delta`; (2) fanout-guard `evaluate('shipstation', correlationId)` denies → `skipped_guard`; (3) workspace v2 defaults missing → `skipped_no_v2_defaults`; (4) variant unknown → `skipped_unknown_variant`; (5) bundle parent → `skipped_bundle_parent` (Phase 2.5(a) seed exclusion); (6) ledger short-circuit on duplicate retry. Ledger row in `external_sync_events` keyed `(system='shipstation_v2', correlation_id, sku, action='increment'|'decrement')` — duplicate enqueues from sibling paths (sales via `shipstation-v2-decrement`, manual counts direct from `submitManualInventoryCounts`, generic fanout) all dedup safely against this UNIQUE. v2 transaction_type chosen by sign: `delta > 0` → `'increment'` quantity:|delta|; `delta < 0` → `'decrement'` quantity:|delta|. Phase 0 Patch D2 contract honored — never `modify`. Fire-and-forget from callers; failure to enqueue is non-fatal because Phase 5 reconcile sensor catches drift. Echo-cancellation (Rule #65): `fanoutInventoryChange()` skips this enqueue when `source ∈ {shipstation, reconcile}` because those events already represent v2 state. |
| `megaplan-spot-check` | `src/trigger/tasks/megaplan-spot-check.ts` | `0 * * * *` — **Phase 6 closeout (2026-04-18, Saturday Workstream 1):** automated cross-system inventory verification. Pinned to `shipstation` queue (concurrency 1, shares the v2 60 req/min budget with seed/decrement/reconcile). Per workspace: calls `megaplan_sample_skus_per_client(p_workspace_id, p_per_client, p_exclude_count_in_progress, p_prioritize_recent_activity_hours)` RPC (PL/pgSQL, `security definer`, granted to `service_role`) to pick 5 SKUs daily / 15 during ramp window (any workspace at `fanout_rollout_percent < 100` triggers the larger sample). For each SKU reads DB (`warehouse_inventory_levels.available`), Redis (`getInventory(sku).available`), ShipStation v2 (`listInventory({ skus:[sku] })` summed across locations — Patch D2 finding: SKUs at 0 are missing from `/v2/inventory` and treated as 0), and Bandcamp (`bandcamp_product_mappings.bandcamp_origin_quantities` summed across origins). Classification: `agreed` (all four/five match), `delayed_propagation` (DB=Redis, externals lag), `drift_minor` (max diff ≤ 2), `drift_major` (max diff > 2 OR any legacy source unreadable). Persists artifact to `megaplan_spot_check_runs(workspace_id, started_at, finished_at, sampled_sku_count, drift_*_count, summary_json, artifact_md)`. Persistence rule (review pass v4 §5.3): `drift_major` SKUs upsert into `warehouse_review_queue` (`category='megaplan_spot_check'`, `severity='critical'`, `group_key='megaplan-spot-check-{workspace_id}'`) ONLY when the same SKU was drift_major in the immediately prior run — eliminates transient lag noise. **B-4 / HRD-15 5-source extension (2026-04-22):** added Shopify-direct as a 5th data source. Pre-batches Shopify probes per `client_store_connections` row (Shopify only, `do_not_fanout=false`) capped at `MAX_SHOPIFY_DIRECT_PROBES_PER_CONNECTION = 50` per run (high-SKU stores rotate which subset is sampled across runs); resolves `client_store_sku_mappings.remote_inventory_item_id` then calls `getInventoryLevelsAtLocation()` from `src/lib/server/shopify-connection-graphql.ts` so the GraphQL `inventoryLevels` cost stays inside the per-second budget. Symmetric classification adds two new informational states: `legacy_drift` (Shopify+DB agree, ShipStation v2 disagrees — diagnostic only, NOT escalated to the review queue; tells us where SS v2 was wrong before cutover) and `bandcamp_drift` (Shopify+DB+SS agree, Bandcamp disagrees). A Shopify-direct mismatch >2 escalates immediately to `drift_major` regardless of the other sources. Aggregate count of successfully-probed SKUs lands on `megaplan_spot_check_runs.shopify_direct_available`; per-SKU values + per-connection probe counts/failures live in `summary_json` under `rows[].shopifyDirectAvailable` / `shopifyDirectProbeCount` / `shopifyDirectProbeFailures` / `legacyDriftCount` / `bandcampDriftCount`. Op surface: `/admin/settings/megaplan-verification`. Switch cron to `0 9 * * *` after Tuesday onboarding for daily cadence. |
| `deferred-followups-reminder` | `src/trigger/tasks/deferred-followups-reminder.ts` | `0 9 * * *` (UTC) — **Phase 6 closeout (2026-04-18, Saturday Workstream 1):** daily reminder cron. Reads `docs/DEFERRED_FOLLOWUPS.md` (bundled into Trigger build via `additionalFiles` extension in `trigger.config.ts`). Parses YAML front matter as a sequence of `{ slug, title, due_date, severity, context }` entries. For every entry whose `due_date <= today` (ISO date compare), upserts one `warehouse_review_queue` row per workspace (`category='deferred_followup'`, `group_key='deferred-followup-{slug}'` — UNIQUE per migration `20260401000003_scraper_hardening.sql` so the cron is fully idempotent). Operator workflow: edit the markdown YAML to add/extend/close entries, then run `update warehouse_review_queue set status='resolved' ...` for the closed slug. Initial registry covers: Phase 7 dormant cleanup (90 days), Tier 1 #9/#10 waivers (30 days), `external_sync_events` retention verification (1 week), ShipStation stale location cleanup (~33 days), plus four v6 codebase verification findings (migration ordering, shared utils path, ROLE_MATRIX rename, scanning auth audit). |
| `inventory-committed-counter-recon` | `src/trigger/tasks/inventory-committed-counter-recon.ts` | `15 4 * * *` (UTC) — **Phase 5 §9.6 D1.c (2026-04-24):** daily counter↔ledger reconciliation for the Phase 5 D1 ATP substrate. NO queue pin — read-only sweep across all workspaces, paginates `warehouse_inventory_levels` and OPEN `inventory_commitments` (`released_at IS NULL`) at 1000 rows / page. For each `(workspace_id, sku)` compares the denormalized `warehouse_inventory_levels.committed_quantity` counter against the in-memory SUM of open ledger rows. Drift is reported, NEVER auto-corrected (auto-correction would mask the underlying trigger bypass / lost write that the drift is signaling). Drifted SKUs upsert into `warehouse_review_queue` (`category='inv_committed_counter_drift'`, `severity='high'`, `group_key='inv-committed-drift:{ws}:{sku}'` — UNIQUE constraint `uq_review_queue_group_key` means repeats increment occurrence_count rather than spawn dupes). Per-workspace summary `sensor_readings('inv.committed_counter_recon')` rows (healthy / warning) for dashboard time-series. Detection runs UNCONDITIONALLY — independent of `workspaces.atp_committed_active` because trigger correctness is independent of consumer-side math. Drift catches: (1) trigger bypass, (2) trigger DISABLE for maintenance, (3) raw-SQL writes against the ledger, (4) NULL leak on the counter column. Pure-data core extracted as `runInventoryCommittedCounterRecon({ supabase, reconRunId, pageSize })`; the `schedules.task` is a thin wrapper. Tests: `tests/unit/trigger/inventory-committed-counter-recon.test.ts` (8 cases). |
| `bulk-create-locations` | `src/trigger/tasks/bulk-create-locations.ts` | Triggered on demand from `createLocationRange` Server Action when the requested range exceeds the inline cap (>30 entries) — **Saturday Workstream 3 (2026-04-18):** ranged warehouse-location creation + ShipStation v2 mirror. Pinned to `shipstation` queue (concurrency 1, shares the v2 60 req/min budget). `maxDuration: 600` (Vercel Server Action timeout safe). Per location: insert `warehouse_locations` row → if `workspaces.shipstation_v2_inventory_warehouse_id` is set call `createInventoryLocation` (resolving 409 conflicts via `listInventoryLocations`) → write `shipstation_inventory_location_id`/`shipstation_synced_at` on success or `shipstation_sync_error` on failure → throttle. Respects `workspaces.shipstation_sync_paused`: when true, skips the v2 mirror and records `paused`. Failures surface as a single `warehouse_review_queue` row at task exit (`category='location_bulk_mirror_partial'`, `severity='medium'`) carrying the per-row failure summary. Local row creation never blocks on v2 — operators can backfill via `retryShipstationLocationSync`. |
| `ramp-halt-criteria-sensor` | `src/trigger/tasks/ramp-halt-criteria-sensor.ts` | `*/2 * * * *` — **Phase 6 finish-line v4 (2026-04-13):** auto-halt sensor for the bidirectional fanout ramp. Reads `sensor_readings` within the last 1h activity window, filters STRESS-* / `metadata->>'stress_run_id'` rows, computes spot-check `drift_major` fraction (from `megaplan_spot_check`) and ShipStation v2 5xx rate (from `shipstation_v2_5xx_rate` — producer deferred per slug `phase-6-shipstation-v2-5xx-rate-sensor`), then evaluates §31 halt criteria via the pure evaluator `src/trigger/lib/ramp-halt-evaluator.ts` (H-1..H-5). On `halt` or `halt_and_page` AND `fanout_rollout_percent > 0`, calls `setFanoutRolloutPercentInternal({ percent:0, actor:{kind:'sensor', sensorRun:ctx.run.id} })` directly (NOT the staff Server Action — Trigger tasks lack a Supabase auth session) and writes a `warehouse_review_queue` row (severity `critical` for halt_and_page else `high`). §5.3 two-consecutive-runs persistence on H-3 (spot-check) lives in `workspaces.ramp_sensor_state.lastSpotCheckTripped`. Always emits a `ramp_halt_evaluator` `sensor_readings` row for audit. Tighten cron to `*/15 * * * *` post-ramp per Phase 8e (deferred). 14 evaluator + 8 helper unit tests green. |
| `webhook-events-recovery-sweep-schedule` | `src/trigger/tasks/webhook-events-recovery-sweep.ts` | `*/5 * * * *` — **HRD-17.1 belt-and-braces sweeper:** picks up `webhook_events` rows older than 2 minutes still stuck in `'received'` or `'enqueue_failed'` and re-fires `process-client-store-webhook` with a GLOBAL-scope `idempotencyKey` (HRD-29) so route-handler dispatches and sweeper dispatches collide instead of double-processing. Caps at 100 rows / pass to bound `maxDuration: 120`. **R-3 platform filter (2026-04-23, support-email audit fallout):** sweeper now narrows to `platform IN ('shopify','woocommerce','squarespace')` (`SWEEP_PLATFORMS` constant). Without this filter the sweeper was picking up orphaned `platform='resend'` rows (and would have done the same for `easypost` / `aftership` / `stripe`), dispatching them to `process-client-store-webhook` (which understands none of those payload shapes), then flipping `status='enqueued'` anyway — making genuinely-broken rows appear processed in audit logs and masking the inbound-email failure for ~3 weeks. Each non-client-store platform owns its own ingress (`/api/webhooks/resend-inbound`, `/api/webhooks/easypost`, etc.) and does not need this sweeper. Companion on-demand task `webhook-events-recovery-sweep` shares the same body for manual re-runs. |
| `clandestine-shopify-sync` | `src/trigger/tasks/clandestine-shopify-sync.ts` | `30 4 * * *` — **Phase 0.7 (2026-04-17):** daily distro discriminator. Walks the Clandestine Shopify catalog and creates `warehouse_products` rows with `org_id IS NULL` for products whose variant SKUs are NOT enrolled in `bandcamp_product_mappings`. Skips already-known products and skips Bandcamp-overlapping products (those belong to client-owned `bandcamp-sync` flow). Catalog-only — never writes inventory (Rule #59). Inventory for distro items still arrives via SHIP_NOTIFY (Phase 2). Resolves the missing `if (!orgId) continue;` carve-out in `shopify-sync` so distro products no longer fall through the cracks. |

## Event/On-Demand Tasks

| Task ID | File | Typical Invoker(s) |
|---|---|---|
| `process-shopify-webhook` | `src/trigger/tasks/process-shopify-webhook.ts` | Currently retained for controlled replay/future re-enable. First-party `/api/webhooks/shopify` ingress is observe-only for inventory/order topics and does not enqueue this task in normal operation. |
| `process-shipstation-shipment` | `src/trigger/tasks/process-shipstation-shipment.ts` | `/api/webhooks/shipstation` — **Phase 2 (2026-04-13):** SHIP_NOTIFY processor. Pinned to `shipstation` queue (concurrency 1). Fetches the v1 shipment list at the resource_url via `fetchShipmentsByResourceUrl()`, skips voided shipments, resolves org via `matchShipmentOrg()` (3-tier: store mapping → alias → SKU; unresolvable → review queue `severity:high`, no inventory writes). For each non-zero line item, resolves the SKU via `findVariantBySkuOrAlias()` (3-tier: direct → `client_store_sku_mappings.remote_sku` → `sku_remap_history.from_sku`); unknown SKUs land in the review queue at `severity:medium` and the rest of the shipment continues (per-line failure isolation). Inventory decrement flows through the canonical write path: `recordInventoryChange({ source: 'shipstation', correlationId: 'ssv1:shipment:{shipmentId}:{sku}', metadata: { source_subtype: 'ship_notify', … } })` — Redis SETNX + `record_inventory_change_txn` RPC enforce idempotency on retries. Bandcamp / client-store fanout happens automatically inside `recordInventoryChange` via the in-process fanout step (Rule #48 — never call APIs directly here). Updates `webhook_events.status` to `processed` / `fetch_failed` / `no_shipments` and merges per-shipment results into `metadata` without clobbering the route's original payload. |
| `process-client-store-webhook` | `src/trigger/tasks/process-client-store-webhook.ts` | `/api/webhooks/client-store` — **Phase 0.8 (2026-04-17):** drops payloads from dormant connections early, marks the `webhook_events` row `status = 'dormant_skipped'`. Gate: `shouldFanoutToConnection()`. **Phase 3 handlers (2026-04-22, direct-Shopify cutover):** topic dispatcher routes by suffix in this order — `refund` → `handleRefund`, `cancel` → `handleOrderCancelled`, `inventory`/`stock` → `handleInventoryUpdate`, `order` → `handleOrderCreated` (cancel/refund MUST run before order, since `orders/cancelled` would also match `topic.includes("order")`). **`handleInventoryUpdate`** is platform-aware: Shopify path resolves `inventory_item_id` → SKU via `client_store_sku_mappings.remote_inventory_item_id` (HRD-03), gates on `client_store_connections.default_location_id` (HRD-05 — events at non-default locations persist as `status='wrong_location'` and are NOT applied). **Phase 4 SKU-AUTO-24 demotion-rehydrate path (2026-04-26):** BEFORE returning `sku_mapping_missing`, a missing mapping is first routed through `rehydrateWebhookInventoryUpdate()` (`src/lib/server/webhook-rehydrate.ts`) which looks up `client_store_product_identity_matches` for `(connection_id, remote_inventory_item_id)`, then (in cascade) `(remote_product_id, remote_variant_id)`, then `remote_fingerprint`. When an ACTIVE identity row is found in `outcome_state='client_stock_exception'` and the inbound stock + warehouse ATP + `stock_stability_readings` `boost` window all pass the pure `decideRehydrateAction()` policy (`src/lib/server/webhook-rehydrate-policy.ts`), the handler re-promotes the row to a live alias via `promoteIdentityMatchToAlias()` with `reason='stock_positive_promotion'` and halts the webhook with `status='rehydrate_promoted_alias'`. Other outcomes halt with `rehydrate_evidence_only` (non-exception row, evidence refreshed), `rehydrate_bumped_evidence` (exception row, gate failed — evidence re-observed), `rehydrate_promotion_blocked` (promotion wrapper refused — e.g. OCC conflict or `sku_live_alias_autonomy_enabled=false`), `rehydrate_run_open_failed`, or `sku_autonomy_emergency_paused` (top-level kill switch). `no_identity_row` and `identity_lookup_failed` are fail-open: they fall through to the historical `sku_mapping_missing` return so downstream unknown-SKU discovery still triggers on genuine new listings. The rehydrate orchestrator opens/closes a `sku_autonomous_runs` row (`trigger_source='stock_change_trigger'`) and writes a `sku_autonomous_decisions` audit row on every promotion. WC/Squarespace path preserved (data.sku + data.quantity). Echo cancellation (Rule #65) keyed on resolved SKU. **`handleRefund`** (Shopify `refunds/create`) iterates `refund_line_items`, only `restock_type === 'return'` re-credits via `recordInventoryChange({ source: 'shopify', delta: +qty, correlationId: 'refund:{eventId}:{rli.id}' })`; HRD-07.2 empty-array defense; SKU resolution via `warehouse_order_items.shopify_line_item_id` lookup with `client_store_sku_mappings.remote_sku` fallback for orders that landed before the connection went active; unresolvable SKUs → `warehouse_review_queue` (`category='refund_partial_apply'`, `group_key='refund_partial:{eventId}'`). **`handleOrderCancelled`** (Shopify `orders/cancelled`) — **F-1 / HRD-08.1 partial-cancel recredit (2026-04-22):** re-credits per-line `qty - fulfilled_quantity` (NOT the full line) via `recordInventoryChange({ source: 'shopify', delta: +remaining, correlationId: 'cancel:{eventId}:{sku}:{lineItemId}' })` so a partially-shipped-then-cancelled order does not double-credit the units that already left the warehouse. The `warehouse_order_items.fulfilled_quantity` column is the canonical source of truth; when the inbound `orders/cancelled` payload's per-line `fulfillment_status` disagrees with the DB row a `db_webhook_disagree` flag is emitted on the cancel telemetry row and the DB value wins. `warehouse_order_items.fulfilled_quantity` itself is populated by `handleOrderCreated` from `line_items[i].fulfillment_status === 'fulfilled'` (write-only from this handler — Server Actions, admin tools, and backfill scripts must NEVER mutate it; CI guard `scripts/check-fulfilled-quantity-writers.sh` enforces). Flips `warehouse_orders.fulfillment_status = 'cancelled'`; second delivery short-circuits via `'already_cancelled'`. **Bonus fix in same commit:** `handleOrderCreated` populates `warehouse_order_items.shopify_line_item_id` from `li.id` so the refund + cancel handlers can resolve back. **B-3 / HRD-14 freshness clock (2026-04-22):** every successful event completion (processed OR `echo_cancelled`) now stamps `client_store_connections.last_webhook_at = now()` AND increments the per-topic counter in `client_store_connections.webhook_topic_health` JSONB (snapshot, not history; overwritten per event). This is the SOLE freshness clock consumed by the Channels webhook health card — `webhook_events.last_seen_at` (HRD-01 monotonic guard) is intentionally NOT joined into the card to avoid two competing freshness fields. The stamp is best-effort: a write failure logs but does not fail the webhook task. **Phase 8 (2026-04-27, SKU-AUTO-3 / SKU-AUTO-21):** `handleOrderCreated` calls `runHoldIngressSafely` after `warehouse_order_items` insert (`source: "webhook"`, shared module `src/lib/server/order-hold-ingress.ts`). When `non_warehouse_order_hold_enabled` is on and not emergency-paused, this chains `loadNormalizedOrder` → `evaluateOrderForHold` → `applyOrderFulfillmentHold` (same-tx `p_commit_lines` as hold stamp). On `hold_applied`, skips legacy `commitOrderItems`, decrements only committable remote SKUs, persists ingress warnings to `sensor_readings`, may enqueue `send-non-warehouse-order-hold-alert`. Loader/evaluator/RPC failures fail-open to the pre-Phase-8 path with sensor warnings. |
| `process-client-store-webhook` Woo repair | `src/trigger/tasks/process-client-store-webhook.ts` | **2026-04-28:** Woo `product.*` events with numeric `stock_quantity` route to inventory handling, but only after an active `client_store_sku_mappings` row exists for `(connection_id, remote_sku)`. Unmapped stock updates create a `woo_unmapped_stock_update` review queue item and return `sku_mapping_missing` without inventory writes. Woo order webhook ingestion uses the same idempotency key and remote-modified reconciliation contract as polling (`ingestion_idempotency_key`, `external_order_modified_at`). |
| `aftership-register` | `src/trigger/tasks/aftership-register.ts` | `create-shipping-label` — **Legacy, sunset path.** New label flow registers via `easypost-register-tracker` (see below). |
| `easypost-register-tracker` | `src/trigger/tasks/easypost-register-tracker.ts` | `create-shipping-label` (tracking-notification hardening v5, 2026-04-25) — Calls EasyPost `Tracker.create({ tracking_code, carrier })`, persists `easypost_tracker_id` + `easypost_tracker_status` to `warehouse_shipments` via `updateShipmentTrackingStatusSafe` (RPC enforces sticky terminal states). Idempotent on `tracking_number+carrier`. Failures surface as `warehouse_review_queue` items at `severity='medium'` and re-enqueue with backoff; the parity sensor `tracking.status_drift_24h` flags any shipment that stays untracked > 24h. Pinned to default queue. |
| `send-tracking-email` | `src/trigger/tasks/send-tracking-email.ts` | EasyPost webhook (`/api/webhooks/easypost`) on `tracker.updated` transitions, plus `easypost-register-tracker` for `OUT_FOR_DELIVERY` / `DELIVERED` transitions. **Tracking-notification hardening v5 (2026-04-25):** Insert-pending-FIRST contract — a `notification_sends` row is inserted with `status='pending'` BEFORE the Resend send call (Slice 2 idempotency guarantee — a process crash between insert and send is recovered by `notification-failure-sensor` rather than producing a phantom missing email). Idempotency triple-defense: (1) `notification_sends` partial unique index on `(workspace_id, shipment_id, kind, idempotency_key)`, (2) Resend SDK `idempotencyKey` header, (3) `notification_provider_events.provider_event_id` dedup. All status transitions route through `updateNotificationStatusSafe`. `ResendSendError` is classified into transient vs terminal — transient retries with backoff; terminal flips to `provider_failed` and emits `notification.send_failure` Sentry event. Renders the hardened tracking email template (brand-color sanitization, image-URL allowlist, public destination PII allowlist via `pickPublicDestination`). |
| `notification-failure-sensor` | `src/trigger/tasks/notification-failure-sensor.ts` | Cron `*/15 * * * *` + manual operator trigger via `triggerNotificationFailureSensor` Server Action (tracking-notification hardening v5 ops surface). Detects `notification_sends` rows stuck in `pending` (>15min after `created_at`) or `provider_failed` (>1h since last attempt with retries < 5), and shipments whose `easypost_tracker_status` indicates `delivered` but no terminal notification row exists. Stuck rows surface on `/admin/operations/notifications` for operator-driven retry/cancel. Sensor reads via `created_at` (not `sent_at`, which is NULL for failed sends) — see updated `unified-shipping-sensors.ts` for the `notification.send_failure_rate_24h` / `resend.bounce_rate_24h` / `resend.complaint_rate_24h` metrics that count `delivered`/`provider_failed` alongside legacy `sent`/`failed` for backward compatibility. |
| `shopify-full-backfill` | `src/trigger/tasks/shopify-full-backfill.ts` | `src/actions/shopify.ts` |
| `shopify-image-backfill` | `src/trigger/tasks/shopify-image-backfill.ts` | `src/actions/admin-settings.ts::triggerShopifyImageBackfill` — manual only, no cron. Fixes two historical data gaps: (1) backfills `warehouse_product_images` rows from `images` JSONB for products that were synced from Shopify before per-row images were hydrated; (2) pushes `bandcamp_art_url` via `productCreateMedia` for products with `images = '[]'` (created by bandcamp-sync before the `media`→`files` field fix). Idempotent. |
| `bandcamp-sync` | `src/trigger/tasks/bandcamp-sync.ts` | `src/actions/bandcamp.ts`, `bandcamp-sync-cron` — **2026-04-19 dedup guard:** unmatched merch checks `bandcamp_product_mappings` by `(workspace_id, bandcamp_item_id)` before SKU lookup, and package-level backstop index `uq_bandcamp_mappings_workspace_item_id` enforces one mapping per package per workspace. Incident containment guard: when `workspaces.bandcamp_sync_paused=true`, task returns before any merch processing. Unmatched create now has definitive mapping re-check before external create and auto-archives newly created Shopify product if DB attach verification fails. |
| `bandcamp-scrape-page` | `src/trigger/tasks/bandcamp-sync.ts` | `bandcamp-sync`, `bandcamp-scrape-sweep`, `scraper-reconcile`, `src/actions/bandcamp.ts` — **hardened 2026-04-14**: category-gated enrichment, per-domain circuit breaker (DB-backed AIMD), dead URL lifecycle (active/probation/dead), inline review queue auto-resolve, adaptive rate limiting |
| `bandcamp-order-sync` | `src/trigger/tasks/bandcamp-order-sync.ts` | `bandcamp-order-sync-cron` — persists `warehouse_orders.shipping_cost` from Bandcamp line shipping, stores per-line `shipping` in `line_items` JSON, repairs existing orders missing `shipping_cost` when API returns shipping. **B-1 per-order idempotency (2026-04-22):** dedup is keyed on Bandcamp `payment_id` (`warehouse_orders.bandcamp_payment_id` lookup) — a BC-issued stable identifier, never a timestamp — so the 30-day getOrders window can overlap prior runs without producing duplicate orders. |
| `bandcamp-mark-shipped` | `src/trigger/tasks/bandcamp-mark-shipped.ts` | `src/actions/bandcamp-shipping.ts`, `bandcamp-mark-shipped-cron` |
| `bandcamp-shipping-verify` | `src/trigger/tasks/bandcamp-shipping-verify.ts` | `bandcamp-shipping-verify-cron` (`*/30 * * * *`, queue: `bandcampQueue`). **Phase 5 (HRD-11) — per-workspace polarity:** runner pre-fetches the set of `workspaces.bc_verify_direct_primary=true` workspaces; runs TWO separate `warehouse_shipments` queries — LEGACY (filters `shipstation_marked_shipped_at IS NOT NULL` + `<= now()-30min`, EXCLUDES direct-primary workspaces via `not.in`) and DIRECT-PRIMARY (drops the SS filter, requires only `created_at <= now()-5min` for inline-push grace). Each row tagged with `_directPrimary`, grouped by (workspace, org), one batched `getOrders` BC API call per group. Sensors split by polarity: legacy → `bandcamp.connector_success` (healthy) / `bandcamp.connector_fallback` (warning); direct-primary → `bandcamp.direct_primary_already_shipped` (healthy, BC already shows shipped) / `bandcamp.direct_primary_push` (HEALTHY, expected steady-state). Per-workspace `trigger:bandcamp-shipping-verify` summary row replaces the earlier global tally. The push (`tasks.trigger("bandcamp-mark-shipped", { shipmentId })`) fires in BOTH modes — polarity decides whether it's the alarm path or the expected path, never whether it happens. Polarity flip is operator-controlled via `flipBandcampPrimaryToDirect` Server Action (HRD-11.1) which hard-checks `workspaces.shipstation_sync_paused=true` AND zero SS-marked BC shipments in last 48h before flipping. |
| `pirate-ship-import` | `src/trigger/tasks/pirate-ship-import.ts` | `src/actions/pirate-ship.ts` — **rewritten 2026-04-13**: multi-tier org matching (exact order → ilike fallback → alias → review queue), two-layer tracking dedup (pre-check + 23505 catch), auto-links `order_id` + copies `bandcamp_payment_id`, creates real `warehouse_shipment_items` from order line items, `label_source='pirate_ship'`, import-level metrics in `errors` JSONB, writes `sensor_readings` on failure. **Hardened 2026-04-10**: `retry: { maxAttempts: 1 }` (file errors not transient), `ctx.run.id` stored in `errors` JSONB on completion, 23505 race condition logged at `warn`, failure `errors` includes `phase` + `timestamp` + `trigger_run_id`. **Updated 2026-04-15**: Tier 3 alias insert explicitly sets `order_id: null, mailorder_id: null` — documents intent, satisfies `chk_shipment_not_both_orders` constraint |
| `inbound-product-create` | `src/trigger/tasks/inbound-product-create.ts` | `src/actions/inbound.ts` |
| `inbound-checkin-complete` | `src/trigger/tasks/inbound-checkin-complete.ts` | `src/actions/inbound.ts` |
| `tag-cleanup-backfill` | `src/trigger/tasks/tag-cleanup-backfill.ts` | `src/actions/admin-settings.ts` |
| `preorder-setup` | `src/trigger/tasks/preorder-setup.ts` | `bandcamp-sync` / scraper paths — adds Pre-Orders + New Releases tags to Shopify, syncs `warehouse_products.tags`, logs failures to `channel_sync_log` + review queue |
| `preorder-release-variant` | `src/trigger/tasks/preorder-fulfillment.ts` | `src/actions/preorders.ts::manualRelease` — single-variant targeted release (not the full daily job) |
| `debug-env` | `src/trigger/tasks/debug-env.ts` | manual diagnostics |
| `create-shipping-label` | `src/trigger/tasks/create-shipping-label.ts` | `src/actions/shipping.ts` (createOrderLabel) |
| `pirate-ship-historical-link-audit` | `src/trigger/tasks/pirate-ship-historical-link-audit.ts` | Order Pages Transition Phase 0 — staff-triggered via `/admin/orders/diagnostics`. One-shot audit that flags `warehouse_shipments` with `label_source='pirate_ship'` whose linked `warehouse_orders.created_at` is more than 180 days BEFORE `coalesce(ship_date, created_at)` (legacy order-number-only matcher artifacts). Surfaces matches as `warehouse_review_queue` rows (`category='pirate_ship_potential_mislink'`, `severity='medium'`, dedup `group_key='pirate_ship_mislink:{shipment_id}'`). NEVER auto-unlinks (would break the customer tracking page). Pinned to default queue. |
| `order-identity-backfill` | `src/trigger/tasks/order-identity-backfill.ts` | Order Pages Transition Phase 1 — staff-triggered via `enqueueIdentityBackfill()` from `/admin/orders/diagnostics`. Resumable / cursor-paged backfill that walks `warehouse_orders` rows with `connection_id IS NULL`, calls the pure `resolveOrderIdentityV2()` resolver against the candidate `client_store_connections`, and stamps `connection_id` / `external_order_id` / `ingestion_idempotency_key_v2` / `identity_resolution_status` accordingly. Bandcamp orders without a deterministic mapping are stamped `bandcamp_legacy_null` so the partial unique indexes never block them. Ambiguous rows (>1 candidate) write a `warehouse_order_identity_review_queue` entry instead of guessing. State persisted to `warehouse_order_identity_backfill_runs` (`status`, `cursor_order_id`, counters); the same run id resumes mid-batch on retry. Pinned to a dedicated `order-identity-backfill` queue (concurrency 1) to keep cutover Postgres pressure bounded. |
| `order-mirror-links-bridge` | `src/trigger/tasks/order-mirror-links-bridge.ts` | Order Pages Transition Phase 2 — staff-triggered via `enqueueMirrorLinksBridge()` from `/admin/orders/diagnostics`. Walks `warehouse_orders` rows that lack a mirror link, batch-pulls candidate `shipstation_orders` by `order_number` / email / time window, and runs the pure `decideMirrorLink()` decider per pair. Upserts diagnostic rows into `order_mirror_links` with `confidence ∈ {deterministic, probable, manual, rejected}` and `match_signals` JSONB. Diagnostic-only — never triggers fanout, label flow, or hold semantics. Pinned to the existing `shipstation` queue (shares the v2 60 req/min budget). |
| `mark-platform-fulfilled` | `src/trigger/tasks/mark-platform-fulfilled.ts` | `create-shipping-label` — **Order Pages Transition Phase 5b additions (2026-04-29):** opens a `platform_fulfillment_writebacks` row + per-line `platform_fulfillment_writeback_lines` rows BEFORE the platform call via `openWriteback()` so a crash mid-call leaves an `in_progress` row visible on Direct Orders detail. Records `succeeded` on success and either `failed_terminal` (on `ShopifyFulfillmentError` with a selection failure) or `failed_retryable` (on transport/throttle/userErrors) on failure; order-level status is derived from line statuses (`partial_succeeded` when some succeed and others fail). Identity hardening: accepts `warehouse_orders.external_order_id` as a fallback when `metadata.platform_order_id` is absent (legacy/historical rows), records `blocked_missing_identity` via `recordBlockedWriteback()` when both are absent. Bandcamp short-circuit records `blocked_bandcamp_generic_path` so Direct Orders renders the explicit reason instead of "no writeback row found"; the `bandcamp-mark-shipped` cron remains the canonical Bandcamp writeback path. **B-2 / HRD-28 GraphQL migration (2026-04-22):** Shopify fulfillment path migrated from REST `POST /orders/{id}/fulfillments.json` to GraphQL `fulfillmentCreate` (Shopify deprecated REST fulfillments in 2024-04). Pure logic lives in `src/lib/server/shopify-fulfillment.ts` so it is unit-testable without the task runner. Step 1: `fetchFulfillmentOrders(orderGid)` enumerates all FOs for the order. Step 2: `selectFulfillmentOrder()` filters to `status IN ('OPEN', 'IN_PROGRESS')` and picks the FO whose `lineItems[].sku` set covers the warehouse order's required SKUs (derived from `warehouse_order_items.fulfilled_quantity`); ambiguous coverage tie-breaks on oldest GID + emits a `sensor_readings` warning so operators see the noise level. Step 3: `runFulfillmentCreateMutation()` treats BOTH a non-empty top-level `errors[]` AND `data.fulfillmentCreate.userErrors[]` as failure (Shopify returns partial data when userErrors fire) — on failure throws `ShopifyFulfillmentError` carrying the raw `errors`/`userErrors` payload + `partialFulfillmentId` and the platform side is NOT marked fulfilled (review queue gets the diagnostic). Per CLAUDE.md Rule #1: this is `fulfillmentCreate` (not `productSet`), so the full-shape builder rule does not apply. |
| `mark-mailorder-fulfilled` | `src/trigger/tasks/mark-mailorder-fulfilled.ts` | `create-shipping-label` |
| `discogs-catalog-match` | `src/trigger/tasks/discogs-catalog-match.ts` | `src/actions/discogs-admin.ts` |
| `discogs-initial-listing` | `src/trigger/tasks/discogs-initial-listing.ts` | `src/actions/discogs-admin.ts` (confirmMapping) |
| `discogs-message-send` | `src/trigger/tasks/discogs-message-send.ts` | `support-message-delivery` for Discogs-originated support conversations; preserved for manual/staff callers. |
| `support-message-delivery` | `src/trigger/tasks/support-message-delivery.ts` | `src/actions/support.ts::sendMessage` after `support_message_deliveries` rows are inserted. Processes pending/failed delivery rows by `messageId`, sends email via Resend or delegates Discogs sends to `discogs-message-send`, updates provider IDs/status/error fields, and writes delivery events for timeline/dead-letter visibility. |
| `catalog-stats-refresh-demand` | `src/trigger/tasks/catalog-stats-refresh.ts` | Staff admin UI (on-demand) |
| `bandcamp-sales-backfill` | `src/trigger/tasks/bandcamp-sales-backfill.ts` | Staff admin UI (on-demand); self-triggers in yearly chunks (DEPRECATED — use scripts/run-sales-backfill.mjs). Pinned to `bandcampQueue` for defense-in-depth (Phase 0.0 hotfix 2026-04-13) even though the body now only logs and returns. |
| `bundle-component-fanout` | `src/trigger/tasks/bundle-component-fanout.ts` | `bandcamp-sale-poll` (when bundle variant sold) |
| `bundle-derived-drift-sensor` / `bundle-derived-drift-sensor-schedule` | `src/trigger/tasks/bundle-derived-drift.ts` | Hourly cron (`0 * * * *`) — **Phase 2.5(c) (2026-04-13):** detector that compares ShipStation v2's stored `available` per bundle SKU against the value derived via `computeEffectiveBundleAvailable(bundleStock, components, componentInventory)` (the shared helper from `src/lib/server/bundles.ts` introduced in Phase 2.5(b)). Pinned to `shipstation` queue (concurrency 1, shared with seed and SHIP_NOTIFY processing — keeps total v2 read budget bounded). Per workspace: loads bundle parent variants and their components, bulk-loads `warehouse_inventory_levels` for bundle + component variants, batch-fetches v2 inventory for bundle SKUs (`listInventory({ skus })`), and for any v2 row whose `available` differs from the derived value by more than `BUNDLE_DRIFT_TOLERANCE` (default 2 units) opens a `warehouse_review_queue` row keyed `group_key='bundle.derived_drift:{workspace_id}:{sku}'` (Rule #55 — re-detection bumps `occurrence_count`). Bundle SKUs that v2 does NOT return are silently skipped — Phase 2.5(a) excludes bundles from the seed task by design, so a missing v2 row is the desired state. Sensor never writes inventory; Rule #20 write-path is untouched. |
| `bandcamp-tag-backfill` | `src/trigger/tasks/bandcamp-tag-backfill.ts` | Staff admin UI (on-demand); scrapes album HTML for `<a class="tag">` genre tags |
| `shipstation-seed-inventory` | `src/trigger/tasks/shipstation-seed-inventory.ts` | `src/actions/shipstation-seed.ts::triggerShipStationSeed` (real run) and `previewShipStationSeed` (dry-run) — **Phase 3 (2026-04-13):** one-shot ShipStation v2 inventory seed per workspace. Pinned to `shipstation` queue (concurrency 1, shared with `process-shipstation-shipment`). Selection cascade: variant has `org_id IS NOT NULL` (fulfillment client only — distro `org_id IS NULL` excluded) → not a `bundle_components.bundle_variant_id` (Phase 2.5(a) bundle exclusion) → `bandcamp_product_mappings.push_mode = 'normal'` → `computeEffectiveBandcampAvailable(bandcamp_origin_quantities) > 0` (NOT the customer-facing TOP `quantity_available` — Part 9 audit) → `warehouse_inventory_levels.available > 0`. Push value = `warehouse_inventory_levels.available` via `adjustInventoryV2({ transaction_type: 'increment' })` per SKU. Idempotent via `external_sync_events` ledger keyed by `(system='shipstation_v2', correlation_id='seed:{workspace_id}:{run_id}', sku, action='increment')` — retries with the same `ctx.run.id` short-circuit on duplicate insert. `dryRun: true` skips the ShipStation call and reports the gate counts only. Bulk-shaped operation (Rule #59) — does NOT route through `recordInventoryChange()`; the Phase 5 reconcile sensor catches drift. Logs a `channel_sync_log` row with `channel='shipstation_v2'`, `sync_type='seed_inventory'` for the admin "Recent runs" panel. |
| `shipstation-store-refresh` | `src/trigger/tasks/shipstation-store-refresh.ts` | `src/actions/clandestine-shopify.ts` (future wiring after Clandestine draft saves) — **Phase 3 (2026-04-13) STUB:** manual ShipStation store refresh. Pinned to `shipstation` queue. Currently returns `status: 'deferred'` with reason `endpoint_tbd_using_24h_auto_import_fallback` because Open Question #2 (public refresh endpoint existence) is unresolved — candidates are the undocumented `POST /stores/refreshstore` v1 path or the UI-only "Refresh Store" button. The stub still records the request in `channel_sync_log` so operators can see refresh attempts; the 24h auto-import + Phase 5 reconcile sensor cover the gap. Wiring sites can call this today; only the internal implementation flips when the endpoint is confirmed. |
| `sku-rectify-via-alias` | `src/trigger/tasks/sku-rectify-via-alias.ts` | `src/actions/sku-conflicts.ts::applyAliasResolution` — **Phase 0.5 (2026-04-17):** invoked SKU rectifier. Pinned to `shipstation` queue, retry 3×. Wraps `GET /products` → merge → `PUT /products/{id}` (full-resource replacement) inside a per-`shipstation_product_id` Redis mutex (`src/trigger/lib/redis-mutex.ts`, TTL 120s — exceeds ShipStation v1 `Retry-After` ceiling). Captures pre-image, uses `addAliasToProduct()` (idempotent merge), verifies post-write alias presence, writes `sku_remap_history` row + `external_sync_events` ledger entry keyed by `(system, correlation_id, sku, action)`. On success marks `sku_sync_conflicts.status='resolved'`. |
| `shipstation-v2-decrement` | `src/trigger/tasks/shipstation-v2-decrement.ts` | **No production caller as of 2026-04-13 second-pass audit** — formerly enqueued by `bandcamp-sale-poll` after each successful `recordInventoryChange()` for a Bandcamp sale; that enqueue was removed because ShipStation Inventory Sync now decrements v2 natively when SS imports the Bandcamp order. Task code preserved as a manual rollback path. To re-activate (e.g. SS Inventory Sync disabled per-workspace): re-add the explicit enqueue in `bandcamp-sale-poll` AND remove `'bandcamp'` from `SHIPSTATION_V2_ECHO_SOURCES` in `src/lib/server/inventory-fanout.ts` together. **Phase 4 (2026-04-13):** sale-poll → ShipStation v2 decrement bridge. Pinned to `shipstation` queue (concurrency 1, shared with `process-shipstation-shipment` and `shipstation-seed-inventory` so v2 60 req/min budget stays serialized). Correlation-id shape `sale:{band_id}:{package_id}:{newSold}:{sku}`. Skip cascade: `fanout-guard.shouldFanout("shipstation", correlationId)` → `workspaces.shipstation_v2_inventory_warehouse_id`/`_location_id` IS NOT NULL → variant resolved → variant is NOT a `bundle_components.bundle_variant_id` → `external_sync_events` ledger acquired (`UNIQUE(system='shipstation_v2', correlation_id, sku, action='decrement')`). Calls `adjustInventoryV2({ transaction_type:'decrement', quantity:|delta| })` — Phase 0 Patch D2 contract: NEVER `modify new_available:0`. On error marks ledger row `error` and re-throws so Trigger.dev retries idempotently against the same correlation_id. |
| `shipstation-bandcamp-reconcile-hot-schedule` / `shipstation-bandcamp-reconcile-warm-schedule` / `shipstation-bandcamp-reconcile-cold-schedule` | `src/trigger/tasks/shipstation-bandcamp-reconcile.ts` | Three Trigger.dev `schedules.task` wrappers (cron `*/5 * * * *`, `*/30 * * * *`, `0 */6 * * *`) plus an on-demand companion `shipstation-bandcamp-reconcile-task`. All four delegate to a single inner `runShipstationBandcampReconcile(tier, payload, ctx, deps)` and are pinned to `shipstation` queue (concurrency 1, shared with `process-shipstation-shipment`, `shipstation-seed-inventory`, `shipstation-v2-decrement`, `bundle-derived-drift-sensor` so the v2 60 req/min budget stays serialized). **Phase 1 Pass 2 D6 (2026-04-21):** added a 60s no-other-writer gate (`RECONCILE_NO_OTHER_WRITER_WINDOW_MS`) — before applying any auto-fix the task calls `fetchMostRecentPushBySku()` against `client_store_sku_mappings` (filtered to `is_active=true`, chunked at 200 SKUs to stay under the PostgREST URL budget) and skips `applyAutoFix` for any SKU whose most-recent `last_pushed_at` is within the window. Drift is still recorded with `skip_reason='recent_other_writer'` + `most_recent_writer_at` and counted in `result.recentWriterGated` so operators see the deferral, but no review-queue row is written either (the gate held BEFORE the severity bucket — we don't yet know whether the drift is real or just an in-flight push). The `now: () => number` dep is plumbed through `ReconcileDeps` for deterministic testing. The 4 D6 cases in `tests/unit/trigger/shipstation-bandcamp-reconcile.test.ts` cover: no mapping rows, mapping outside window, mapping inside window, and multiple mappings where the most-recent stamp wins. **Phase 5 (2026-04-13):** steady-state ShipStation v2 ↔ DB drift detector + auto-fixer. Per workspace: short-circuits with `skipped_no_v2_defaults` when `workspaces.shipstation_v2_inventory_warehouse_id`/`_location_id` are NULL; loads bundle-parent SKUs from `bundle_components` and excludes them (Phase 2.5(c) `bundle-derived-drift-sensor` owns bundles); selects candidates by tier — hot = `warehouse_inventory_levels.available <= 10` UNION `warehouse_inventory_activity` rows with `delta < 0` in last 24h; warm = same but 30-day window; cold = full per-workspace catalog. Batch-fetches v2 inventory via `listInventory({ skus, inventory_warehouse_id, inventory_location_id })` (Rule §7.1.6 — NEVER per-SKU `getInventoryBySku`). SKU-at-zero equality: a v2 row missing for a SKU is treated as `available: 0` (Phase 0 §4.2.3 — v2 omits zero rows); skipped silently if our DB also reports 0. Drift thresholds (plan §7.1.6.1): `|drift| <= 1` silent auto-fix, `2-5` auto-fix + low-severity review queue row, `> 5` auto-fix + high-severity review queue row. Auto-fix flows through `recordInventoryChange({ source:'reconcile', correlationId:'reconcile:{tier}:{run_id}:{sku}' })` — Phase 5 schema migration `20260413000030_phase5_reconcile_and_sku_sync_status.sql` extends `warehouse_inventory_activity_source_check` to admit `'reconcile'`. NEVER writes to v2 (v2 is the source of truth at this layer). Review queue dedup uses `group_key='reconcile.qty_drift:{workspace_id}:{sku}'` (Rule #55 — re-detection bumps `occurrence_count` and may escalate severity); on-fix the row is left OPEN so operators can verify the auto-fix landed. Each tier records a `channel_sync_log` row (`channel='shipstation_v2'`, `sync_type='reconcile_{tier}'`) consumed by the admin monitoring page `/admin/settings/shipstation-inventory`. The on-demand `shipstation-bandcamp-reconcile-task` accepts `{ workspaceId, tier, dryRun? }` and is invokable via `triggerReconcileRun()` Server Action for manual reruns from that page. |
| `bandcamp-push-on-sku` | `src/trigger/tasks/bandcamp-push-on-sku.ts` | `process-shipstation-shipment` (after each per-line successful `recordInventoryChange()`) — **Phase 4 (2026-04-13):** SHIP_NOTIFY → Bandcamp focused per-SKU push. Pinned to `bandcamp-api` queue (concurrency 1 — Rule #9 OAuth serialization) to coexist with `bandcamp-sync`, `bandcamp-inventory-push`, `bandcamp-sale-poll`. Correlation-id shape `ship:{shipmentId}:{sku}`. Skip cascade: `fanout-guard.shouldFanout("bandcamp", correlationId)` → variant resolved → not distro (`warehouse_products.org_id IS NOT NULL`) → variant is NOT a bundle parent → `bandcamp_product_mappings` row exists → `push_mode IN ('normal','manual_override')` → mapping is package-level (option-level mappings defer to the 5-min `bandcamp-inventory-push` cron) → `external_sync_events` ledger acquired (`UNIQUE(system='bandcamp', correlation_id, sku, action='modify')`). Computes `pushed_quantity = MAX(0, available - effective_safety)` from `warehouse_inventory_levels` (identical formula to the cron path), refreshes the OAuth token via `refreshBandcampToken` (queue-serialized), calls `updateQuantities` for the single item. On error marks ledger row `error`; cron `bandcamp-inventory-push` (every 5 min) is the safety net. |
| `shipstation-export` | `src/trigger/tasks/shipstation-export.ts` | `src/actions/shipstation-export.ts::triggerShipstationExport` (admin button at `/admin/settings/shipstation-export`) and `scripts/export-shipstation-import.ts` (CLI) — **2026-04-20:** operator-driven generation of the ShipStation product-import CSV+XLSX (30-column header). Generic queue (no external API calls — pure Postgres reads + Storage uploads). `maxDuration: 600` (full 3K-row build is ~7s; cap leaves 100× headroom). Reads its run row from `shipstation_export_runs` to learn `mode` (full|incremental) and `since_ts`, calls the shared `buildShipstationExport({ supabase, sinceTs })` builder, uploads `{workspace_id}/{yyyymmdd}/{runId}.{csv,xlsx,summary.json}` to the private `shipstation-exports` Storage bucket, and writes back `data_max_ts`/`rows_written`/`duplicates_skipped`/`coverage`/`duplicate_skus` so the next incremental run can chain off `data_max_ts` and the admin UI can render history + signed-URL downloads. Boolean columns emit literal `"true"`/`"false"` (Yes/No triggers per-row WARNING in ShipStation import preview). SKU normalization strips zero-width chars (U+FEFF/U+200B-D/U+2060) before dedup; collisions are reported in the summary, not silently dropped. On error: row is updated to `status='failed'` with the message and the admin Recent runs table surfaces it inline. |

## Domain Touchpoints

- **Inventory:** `process-client-store-webhook`, `multi-store-inventory-push`, `bandcamp-inventory-push`, `redis-backfill` (plus `process-shopify-webhook` for replay/future re-enable only)
- **Orders/shipments:** `client-store-order-detect`, `bandcamp-order-sync`, `bandcamp-mark-shipped`, `create-shipping-label`, `mark-platform-fulfilled`, `mark-mailorder-fulfilled`, `daily-scan-form`
- **Mail-order (consignment):** `discogs-mailorder-sync`, `discogs-client-order-sync`, `mark-mailorder-fulfilled`, `mailorder-shopify-sync`
- **Discogs master catalog:** `discogs-catalog-match`, `discogs-initial-listing`, `discogs-listing-replenish`, `discogs-message-poll`, `discogs-message-send`
- **Catalog/release readiness:** `bandcamp-sync`, `bandcamp-scrape-page`, `preorder-setup`, `preorder-fulfillment`
- **Billing/storage:** `monthly-billing`, `storage-calc`
- **Scraper observability:** `sensor-check` now includes `bandcamp.merch_sync_log_stale`, `bandcamp.scraper_review_open`, `bandcamp.scrape_block_rate`, `bundle.component_unavailable` sensors from `channel_sync_log` + `warehouse_review_queue`. Index: `idx_channel_sync_log_sensor` on `(workspace_id, sync_type, created_at DESC) WHERE status = 'completed'`. `bandcamp-scrape-page` logs per-scrape outcome (HTTP status, retryAfterSeconds) to `channel_sync_log` sync_type `scrape_page`.
- **Support/reliability:** `support-escalation`, `support-message-delivery`, `support-delivery-recovery`, `sensor-check`, `tag-cleanup-backfill`
  - `support-escalation` uses SLA deadline fields + pause/snooze state and cooldown timestamps to prevent reminder spam during active chat sessions.
  - `support-message-delivery` and `support-delivery-recovery` make outbound email/Discogs sends retryable and visible through `support_message_deliveries` instead of direct fire-and-forget sends from Server Actions.
  - Inbound support email routing is owned by `/api/webhooks/resend-inbound` before task handoff; Bandcamp order-notification emails may dispatch `bandcamp-sale-poll-per-connection`, while Bandcamp fan messages stay in Support Inbox and resolve org/order/shipment context through `support-email-resolution.ts`.
- **Customer tracking & notifications (tracking-notification hardening v5, 2026-04-25):** `easypost-register-tracker`, `send-tracking-email`, `notification-failure-sensor`, plus the `notification.send_failure_rate_24h` / `resend.bounce_rate_24h` / `resend.complaint_rate_24h` / `tracking.status_drift_24h` sensors in `unified-shipping-sensors.ts`. State writes for `notification_sends.status` and `warehouse_shipments.easypost_tracker_status` MUST go through `src/lib/server/notification-status.ts` (CI guard `scripts/check-notification-status-writes.sh`). `aftership-register` is the legacy parallel path being sunset — no new code paths should depend on it.
- **OAuth hygiene:** `oauth-state-cleanup`

## Design Notes

- **Cache/freshness contract linkage:** Trigger tasks that mutate operational state are part of the cache correctness path. Task outputs/events must map to query invalidation strategy defined in `docs/system_map/CACHE_ARCHITECTURE.md` (mutation/event invalidation first, TTL fallback).
- **Bulk inventory bypass (Rule #59):** `shopify-sync` writes directly to Redis (bypasses `recordInventoryChange`) for performance during bulk pulls. Downstream fanout to client stores relies on the `multi-store-inventory-push` cron (≤5 min lag). This is intentional — not a bug.
- **Discogs client orders:** `client-store-order-detect` explicitly skips Discogs connections; `discogs-client-order-sync` handles them separately due to OAuth 1.0a auth + different SKU/fanout requirements.
- **Bandcamp sale → inventory chain:** `bandcamp-sale-poll` → `recordInventoryChange()` → Redis fanout → Postgres. Chain is fully instrumented. OQ3 verified 2026-03-28.
- **Diagnostic tasks:** `debug-env` exists as a diagnostic task but is intentionally excluded from the task registry (`index.ts`). It can be triggered manually from the Trigger.dev dashboard.

## Audit Requirement

For any issue in sync/webhooks/inventory/orders/support:

1. Identify the request boundary in `API_CATALOG.md`.
2. List all applicable task IDs from this catalog.
3. Confirm whether the issue is in route ingress, task orchestration, or downstream DB/update logic.
4. Do not finalize diagnosis without a Trigger touchpoint check section.
